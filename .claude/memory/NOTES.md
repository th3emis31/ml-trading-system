# Session notes (append-only)

- 2026-09-12: Claude starter kit installed: CLAUDE.md memory, hooks (backup before edit, check
  after edit), skills /safe-upgrade /smart-entry /verify /improve-loop /brain.
- 2026-09-12: CRT_Dashboard_EA - BE lock/trail start/trail distance now follow ApplySymbolPreset (BTC 190/3750/2250 pts), plus a guard keeping any SL modify outside stoplevel/freezelevel/spread. Compiled 0 errors in all 3 terminals; running instances need re-attach.
- 2026-09-12 (evening): Page audit + fixes on live app. Duplicate server killed (libzmq assert crash); MT4Service lock + stale-reply drain + nested account_info parse; /jarvis-voice JS syntax error; favicon; voice listener lang/no-speech/double-start; signal history recorder (signals.json was frozen at 28 Jun, API truncated it to 10); legacy SELL rows flagged; brain-status NaN; learn Current Signals loader; shadow-sim 5-min cache; analytics helper; fake JARVIS trading recommendations replaced with live signals; grouped shared nav on every page (context processor + after_request injection); THEME_CSS visual upgrade layer.
- 2026-09-13: Self-learning gated (src/model_promotion.py: archive -> train -> score on unseen bars -> keep or restore; refuses synthetic data; decisions in data/learning_decisions.json). Backtest: threshold sensitivity, per-year, batch ALL runs. Dashboard: System Pipeline card, tiles keep price, simulated win rate labelled. First gated run XAUUSD: RF promoted (tie, stale champion), LSTM restored. XAUUSD 2y hourly walk-forward: 1311 trades, PF 0.73, -60.9% -> live strategy loses on gold; autonomy stays off.
- 2026-09-13: Edge research R1 (trees) and R3 (LSTM) done on 1h and 1d for XAUUSD/BTCUSD; no configuration passes the agreed holdout criteria. Best: RF daily PF ~1.7 on 30-35 holdout trades (not enough). Hooks fixed (py() skips Store stub, per-project dup check with baseline). R7a trailing-quantile thresholds running.
- 2026-09-13: Data Feed page (/data-feed, /api/data-feed, /api/data/bars): MT5 broker candles read through the app's own connection (MT5Service.copy_rates, times converted from server clock to UTC), Yahoo fallback, indicators per 15m/1h/4h/1d via src.mtf_data.htf_context, MTF alignment, MT4/MT5/Yahoo quotes. Broker 15m depth: XAUUSD from 2024-07-31, BTCUSD from 2025-04-02 (50k bars). Research engine gained 15m/4h specs, --source, --mtf (closed-bar HTF join), --selection robust.
- 2026-09-13: R10 queue killed for low memory after XAUUSD 4h (holdout PF 0.91, -3.1% vs buy&hold +85%). Fixes: lazy LSTMTrader import in src/train.py (research import 1,030 MB -> 818 MB committed, TensorFlow no longer loaded; 34 tests pass), RESEARCH_JOBS=4. Queue restarted memory-guarded, one symbol per process.
- 2026-09-13: Research paused at the user's request because the PC is low on memory; no research process left running. App healthy, MT4/MT5 connected, autonomy off.
- 2026-09-13: Gold 4H MTF research combined from 7 of 9 pieces (src/research_combine.py): holdout 192 trades, PF 1.29, DD 6.5%, +20.3% (buy&hold +85.4%) - passes PF/trades/drawdown, fails only beat-buy&hold. First candidate worth a follow-up (meta-labelling R6, long/short split). rf balanced/wide pieces killed twice for low memory (~1.2 GB free), even at RESEARCH_JOBS=1.
- 2026-09-13: Gold 1H MTF (logit+xgb pieces) fails: holdout 785 trades PF 0.94, -15.7%, DD 23.8%; even baselines lose badly after costs on 1h. 4H MTF (PF 1.29) remains the lead. Next: gold 15m MTF, then BTCUSD 4h MTF.
- 2026-09-13: Gold 15m MTF (logit pieces only; xgb/tight killed for memory) fails: holdout 163 trades PF 0.92, -2.6%. Gold ranking: 4h MTF PF 1.29 >> 1h MTF 0.94 ~ 15m MTF 0.92. Costs kill the short timeframes. Next: BTCUSD 4h MTF pieces.
- 2026-09-13: BTCUSD 4h MTF (logit+xgb pieces; rf killed for memory) fails: holdout 117 trades PF 0.78, -26.5%, DD 30%, despite 5/5 profitable validation folds. R10 summary: only XAUUSD 4h MTF has a real candidate (PF 1.29, 192 trades, DD 6.5%). Next: R6 meta-labelling / long-short split on XAUUSD 4h MTF.
- 2026-09-13: src/edge_research.load_research_frame() now owns history + research features + closed-bar MTF context; run_edge_research and run_meta_research both call it (meta gained --source/--mtf/--rules/--models). 21 research tests pass. Running R6 meta-labelling XAUUSD 4h --mtf, logit.
- 2026-09-13: R6 meta-labelling XAUUSD 4h MTF (logit): filtered holdout PF 1.41 on 86 trades (+17.9%), but the ML filter adds no value - unfiltered 20-bar breakout (wide exits) did 182 trades PF 1.46 +58.9% DD 7.5% on holdout. Checking unfiltered rules per validation fold before trusting it.
- 2026-09-13: Rule check without ML (scratch script, same folds): on gold 4h every trend/breakout rule loses on validation and wins on holdout -> the 2024-26 holdout is a gold bull-trend regime; good holdout PF there is not proof of skill. ML 4h MTF (val PF 1.07, holdout 1.29) is the only setup positive in both periods, which is the real (small) evidence of edge. BTC 4h: all rules fail on holdout. Nothing is ready for live trading; autonomy stays off.
- 2026-09-13: Checked MetaTrader: CRT_Dashboard_EA reloaded itself after the 12 Sep recompile in both MT4 terminals (log 'uninit reason 2' -> 'loaded successfully' -> 'initialized': Program Files (x86) MT4 at 20:07, CMC MT4 at 20:09, after ex4 compile 20:05/20:07), so the trailing-stop fix is live and no manual re-attach is needed. At 09:56 on 13 Sep the user closed the CMC MT4 terminal and one MT5 terminal; the IC Markets MT4 bridge (12755139) and MT5 stay connected; autonomy/auto_execute False; free RAM ~1.0 GB.
- 2026-09-13: Gold 4h MTF search now complete (9/9 pieces): same pick and same holdout (PF 1.29, 192 trades, +20.3%). One research piece peaks at ~850 MB committed / ~190 MB RAM; the earlier kills were the background-task monitor, and foreground runs finish.
- 2026-09-13: Paper trading (user chose 'paper-trade gold 4H first' over auto-trading): src/paper_trader.py forward-tests the XAUUSD 4h MTF xgb tight q0.90 research model on broker candles via /api/data/bars (forming bars dropped; models trained 240+6 bars back so current and trailing EVs are out-of-sample; exits settled with triple_barrier_outcomes; one position at a time; no order path, test enforces it). State data/paper_trading/xauusd_4h_mtf_xgb_tight_q90.json, log paper_trader.log. Windows task 'SmartEntry Paper Trader' runs scripts/run_paper_trader.cmd hourly (idempotent per closed 4h bar). App: /paper-trading page + /api/paper-trading + POST /api/paper-trading/run (Automation nav). tests/test_paper_trader.py 4 pass. First live decision 2026-09-11 17:00 bar: NO_TRADE (best EV -0.30R < threshold 0.23R). App restarted detached via Start-Process (PID 32596, logs/app_stdout.log, app_stderr.log); autonomy off.
- 2026-09-13: DEMO TRADING ON at the user's request ('Do it now for demo'). src/demo_executor.py (tests/test_demo_executor.py, 15 cases) opens XAUUSD positions for fresh gold 4h model signals on the Vantage demo account 11581419 only (trade_mode 0 + 'demo' server + login match), 0.01 lot, magic 440401, stop+target attached (MT5Service.place_market_order gained optional magic / allow_retry_without_stops=False; new quote/positions/close_position), one position at a time, signal age <= 75 min, idempotent per bar, time-limit close at the 6th bar after the signal via /api/demo-model/sync. App endpoints /api/demo-model/execute|sync (localhost only) and /status; Paper Trading page shows the mode banner, open model position and execution log. Config data/paper_trading/demo_execution.json (enabled true, dry_run false); journal demo_execution_journal.json (contains one manual dry-run check, bar 2026-09-13 08:23). Live dry run verified before switching. Jarvis autonomy/auto_execute remain False (separate path). App restarted via the start_trading.bat keep-alive (PID 43300); a duplicate Start-Process copy from 11:27 was removed.
- 2026-09-13: Strategy Lab (src/strategy_lab.py, tests/test_strategy_lab.py): 5 rule families x exit grid, date-fixed search/validation/holdout per market, gates (profitable in both + 60% of years) before any holdout look, deflated Sharpe on the holdout counting every trial. First run 200 candidates in 20 s on XAUUSD/BTCUSD 4h+1h broker bars. SwingTrendPullback EA backtest fails (see BASELINE). strategies/swing_trend_pullback.md written.
- 2026-09-13: SwingTrendPullback v2.00 rewritten (fixes: risk sizing, time-exit close volume, slippage/filling, closed-bar trailing, restart recovery; options: long/short, trend EMA, ATR stop, TP off, spread filter; two-column dashboard) - compiled 0/0, running on XAUUSD H4 (account 25446287), screenshot verified. Strategy Lab EA-family search: 286 4H / 116 1H variants validated, 0 pass the holdout deflated Sharpe; presets steady (DSR 0.41) and trend_rider (0.62) saved in MQL5/Presets. DSR trial variance now = observed - sampling noise, floored at 25% of the noise. Strategy Lab page /strategy-lab (Pipeline nav) + hourly task 'SmartEntry Strategy Lab' (:20). Started ROCKET features (src/rocket_features.py) as the LSTM replacement.
- 2026-09-13: R12 ROCKET (LSTM replacement) tested on XAUUSD 4h MTF: combined holdout PF 1.02 on 67 trades (-0.2%), worse than xgb MTF without ROCKET (PF 1.29). Kept as an option (--rocket) but no edge shown. System Doctor (src/system_doctor.py, 8 tests) first live run: 9 ok, 2 warn (app auto-trading flags ON - investigating; doctor tasks missing -> created).
- 2026-09-13: System Doctor built (src/system_doctor.py, page /system-doctor, tasks every 30 min + daily 06:30 deep with compile + tests; never trades, only safe fix = recreate missing scheduled task). Autonomy check refined: warns only when app execution is armed (autonomy + auto_execute + active session + asset switch); asset auto switches turned on by a user click at 13:16 are reported as info. CLAUDE.md now lists scheduled tasks, pages, commands and the evidence rules.
- 2026-09-13: Asset auto switches (XAUUSD/BTCUSD auto_enabled) turned off via POST /api/auto-trade/settings at the user's request. Daily Report built (src/daily_report.py, page /daily-report, task 06:45): broker-candle levels, volatility regime, momentum, sessions, MTF bias with reasons, gold-BTC correlation, system brief, schedule. First report 13 Sep: gold bullish 0.54 (1d/4h up, 1h/15m down), RSI 30.3, support 4324.6 / resistance 4434.8; BTC bullish 0.54; correlation 0.64. System Doctor now also watches the Daily Report task and its tests.
- 2026-09-13: Pages rebuilt one by one. Signals (/signals, old at /signals-classic): broker candles via lightweight-charts (served from static/vendor) with exact Entry/SL/TP price lines, HOLD shown as no-trade, confidence %, evidence panel (live model walk-forward runs + gold 4H research model + demo mode), 60 s refresh; page opens in 0.0 s (was 3.4 s). TradingView (/tradingview, old at /tradingview-classic): TradingView widgets (OANDA:XAUUSD, BITSTAMP:BTCUSD), alerts table, step-by-step webhook setup (paid plan + HTTPS tunnel), webhook now requires a shared secret (data/tradingview_webhook.json; 403 without it; secret stripped before storing), missing alert side -> UNKNOWN (was BUY), dashboard test alerts excluded from grading. JARVIS voice: 'enable autonomy' now needs 'confirm enable autonomy' within 2 min (was one click enabling auto_execute); fake 'quick trade executed' and invented 'buy signal 81%/2540' replies replaced (quick trade -> start trade approval flow; buy signal -> live plan + hourly backtest evidence); Start/Quick Trade buttons confirm first; Live System Status panel. User asked to create a TradingView account/share login: declined, explained webhooks need no login here.
- 2026-09-13: Performance page (/performance, Trading nav; src/performance_analytics.py + tests; MT5Service.deal_history): market heatmaps hour x weekday (avg return / range / up share) and month x year from broker candles, rankings; real account trading (MT5 11581419, 11,307 closed deals since 2026-01-03: net -10,477 GBP, PF 0.78, win 68%, max DD -15,471) with equity/drawdown curve, P/L + win-rate heatmaps, best/worst hours, per-magic breakdown and filter; tracker curves (doctor quality, learning accuracy, Strategy Lab per daily report, paper equity, backtest runs); pipelines last runs + scheduled tasks. API cached 15 min (first load 1.6 s, cached 0.01 s). Gold 1H: most active 12-15 UTC; BTC 13-17 UTC.
- 2026-09-13 (late): SwingTrendPullback only (user rule). v2.01: retry market-closed/requote entries, exits and trail within the bar; exits before entries; MinLotMaxRiskPct. MT5 Strategy Tester run in a separate portable copy (C:\Users\th_em\MT5_SwingTrend_Tester, /portable /config:tester_ini\*.ini, ShutdownTerminal) so live terminals were untouched. Results 2018-2026 after swap: H4 steady +128% PF 1.30 DD 23% (best), H1 +117%, H4 trend rider +71% DD 30%, D1 +3.7% (swap). Strategy Lab ignores swap -> overstates multi-day holds. Presets D1_best/H1_best added.
- 2026-09-13 17:14: user re-attached SwingTrendPullback v2.01 on live MT5 (D0E8, demo 25446287) XAUUSD H4 with preset SwingTrendPullback_XAUUSD_H4_tradingview.set; log inputs line verified (EMA21/51, push 15>0.5, tol 0.55, RSI>40, ATR12, swing stop 1.5, 2R, trail 4.5, 150 bars, 1% risk, magic 996611); Algo Trading green. Same terminal also runs V10_EA_TRADING_ONLY and DT_Copy (user declined terminal restart) -> never restart it. EA logs "inputs:" on every init for verification.
- 2026-09-13 (evening): Auto-trader page (/auto-trader) got a "SwingTrendPullback EA" tab (read-only). EA v2.02 exports status every 15 s and all its deals after each trade to %APPDATA%\MetaQuotes\Terminal\Common\Files\SwingTrendPullback\<login>_XAUUSD_996611_{status,deals}.json (never in the tester). src/ea_monitor.py pairs deals into trades, merges them permanently into data/ea/swing_trend_pullback/trades.json (never deletes), parses the terminal Experts log (MQL5/Logs + logs, UTF-16), compares active inputs with SwingTrendPullback_XAUUSD_H4_tradingview.set and runs health checks. API GET /api/ea/swing-trend-pullback (10 s cache, ?refresh=1). Tests tests/test_ea_monitor.py (7). system_doctor._read_json now tolerant of ANSI (shared). v2.02 compiled 0/0 in live folder; needs remove+attach on the chart to load.
- 2026-09-13 17:40: user re-attached SwingTrendPullback v2.02 (TradingView preset). Export verified: Common\Files\SwingTrendPullback\25446287_XAUUSD_996611_{status,deals}.json written; /auto-trader EA tab shows Healthy (8/8 checks ok: running, v2.02, connected, algo on, demo 25446287 balance 95,123 GBP, weekend market closed, inputs = preset, no errors); server offset +3 h; 0 trades yet. Edge audit issues=0.
- 2026-09-13 (evening): /auto-trader visual pass (backup app.py.bak.20260913_174517_autotrader_visual). Each card got an accent class (card sec sec-cyan/blue/amber/violet/green/red/teal/slate) driving top line, heading bar, border, header tint and table header line; neutral stat values with green/red only for P&L/return/win rate (neutral when 0 trades); formatCurrency now "-$38.66"; "Daily P&L" label renamed "Open P&L" (it is equity - balance); real-money balance badges amber ("real" class); Execute Trade orange vs Start green; untitled controls card titled "Session Controls"; checkbox alignment in 4-col row; phone tables scroll; #auto hash opens the Auto Trader tab. No features removed. Edge audit issues=0 both tabs, 1440 and 420 px.
- 2026-09-13 (night): /jarvis-voice fixes (backups *_jarvis_voice 20260913_185429). (1) Focus mode: .jarvis-mic-shell lived inside .container (z-index 1) so the opaque stage (z 8990) covered it and took clicks; setFocus now moves the shell to <body> while focused and back after (Edge check: elementFromPoint at core = mic-btn). (2) Voice: scoreVoice prefers natural/neural/online voices, en-GB male (Ryan/Thomas...), penalises robotic desktop voices; new picker #jarvis-voice-select + Test button, saved in localStorage jarvis_voice_name; Edge picks "Microsoft Ryan Online (Natural) - English (United Kingdom)". (3) Duplicate greeting: startupSequence (in /api/jarvis-ext-js) spoke welcomeJARVIS and then a briefing starting with the time greeting, and on refresh the welcome was deferred by the startup lock and replayed after the briefing; welcomeJARVIS(onComplete, {silent}) now speaks with source 'startup' only when no briefing follows. Edge check: 1 greeting (briefing) on open. No server TTS configured (browser voices only).
- 2026-09-13 (night, cont.): focus-mode core sizing: scale target now min(68% height, 92% width), may shrink (min 0.45), core top 39%; the core's CSS transform transition got stuck at its start (scale 1.3 fallback) when focus opened, so focus mode sets the transform inline and uses "transition: filter" only. Edge check: core 549 px, top 40 px in an 808 px viewport, click hits mic-btn, caption below the core; 1 greeting per open; natural voice Microsoft Ryan Online (Natural) en-GB selected.
- 2026-09-13 (late): Backtesting upgrade. (1) Overnight swap in src/strategy_lab.py (HOLDING_COSTS, rollover_counts at 17:00 New York with Wednesday x3, swap_mode percent|price, Market(swap=, swap_mode=), trades carry nights/swap_pct, records carry cost_model). Measured from MT5 tester deals. (2) atr_len param + EA_SPECS["tradingview"]; `python -m src.strategy_lab ea --spec tradingview [--no-swap] [--swap-mode price]`. (3) Simulator validated vs MT5 tester: 99/103 trades matched. (4) src/strategy_book.py (update/rescore/status), book at data/strategy_lab/strategy_book.json, presets in data/strategy_lab/presets (approved ones also copied to MT5 Presets), run after every hourly search (scripts/run_strategy_lab.cmd: search 35 min, book 15 min), book strategies are the search's first mutation parents (self-learning loop). (5) DEFAULT_MARKETS now XAUUSD+BTCUSD x 15m/1h/4h/1d. (6) Strategy Lab page shows the book; /api/strategy-lab includes "book". (7) tests: test_strategy_lab_swap, test_strategy_book, shared tests/conftest.py bars fixture; doctor TEST_FILES updated. Open: BTC swap not measured (BTC results still spread-only).
- 2026-09-13 (late, cont.): Strategy Lab page/API took 17-19 s: profile showed scipy per-call overhead in deflated_sharpe (5,713 calls). Now numpy moments + lru_cached quantile factor + math.erf (identical results, test_fast_deflated_sharpe_matches_the_scipy_formula) and a per-market variance cache: lab_summary 11 s -> 0.8 s.
- 2026-09-13 (night): AI Employee phase 1 (src/ai_employee.py, tests/test_ai_employee.py, scripts/run_ai_employee.cmd, task "SmartEntry AI Employee" daily 07:15, page /ai-employee, API /api/ai-employee [+ POST proposal/run, local only]). Builds a compact context from the system's own reports (doctor, daily report, strategy book, lab registry summary, SwingTrendPullback via ea_monitor, paper trader/demo journal, BASELINE rows, its memory and earlier proposals), runs `claude -p --output-format json --no-session-persistence --restricted --strict-mcp-config --permission-mode dontAsk --permission-prompts none --model sonnet --allowedTools Read,Grep,Glob` on the Max subscription (ANTHROPIC_API_KEY/AUTH_TOKEN/CLAUDECODE removed from the child env), validates the JSON brief, merges repeated proposals (title-word Jaccard >= 0.6, any status), dedupes memory notes, keeps runs.json; lock + once per day. System Doctor check "AI employee" (info before first run, warn on failed/stale run). First real run 2026-09-13 21:02 UTC: ok in 153 s, 3 turns, 7 findings, 5 proposals, 5 notes, health healthy. Its RAM "leak" finding was caused by our own test runs at the time.
- 2026-09-14: Strategy book watchlist grew 44 -> 421 overnight (8 new per market per hourly update). Added cap_watchlist (WATCHLIST_PER_MARKET = 10): best by deflated Sharpe, then lower Monte Carlo loss, holdout PF, trades; the rest get status "archived" (kept with evidence, archived_at/archived_reason, not re-checked hourly, not search parents, never re-added). Applied inside update_book after each market and via `python -m src.strategy_book cap`. After the 08:20 run: 60 watchlist, 392 archived, 0 approved. Page chip shows the archived count; book_summary lists only non-archived entries.
- 2026-09-14: TradingView daily plan = Pine v5 indicator strategies/tradingview/smartentry_daily_plan.pine ("SmartEntry Plan"): SwingTrendPullback rules with the live EA's TradingView inputs (EMA21/51, push 15 > 0.5 ATR, tol 0.55, bull close, RSI14>40, ATR12, swing stop 1.5 ATR, 2R, trail 4.5 ATR, 150 bars); draws entry/stop/target (active trade or "IF pullback" plan), prev-day H/L/C, 5/20/55-day H/L, swing pivots, buy zone, BUY/TP/SL markers, alerts and a plan table; daily levels via request.security lookahead_on with [1] (no repaint). No TradingView/browser tool is connected to Claude; the user pastes the script into the Pine Editor (no account access used). Not compiled locally (no Pine compiler): fix any TradingView compile error the user reports.
- 2026-09-14 (safe-upgrade): POST /webhook/tradingview (app.py) + src/tradingview_intake.py + tests/test_tradingview_intake.py (5 tests, in doctor TEST_FILES). Flow: JSON body -> constant-time shared secret (body secret/passphrase/token or X-Webhook-Secret; stripped; bytes compare) -> existing _store_tradingview_alert (parse_tradingview_alert + build_tradingview_plan model alignment + 100-alert history) -> evaluate_tradingview_alert (tradable side, allowed symbol, entry/SL/TP present and ordered, RR >= 1.5, not Divergent from model, confidence >= 0.5, alert age <= 15 min, not dashboard-test) -> data/tradingview_intake_journal.json (500 rows; GET /api/tradingview/intake). dry_run is the default; only "dry_run": false AND data/tradingview_webhook.json "allow_approval_queue": true (OFF, not enabled) sends an accepted alert to the auto-trader approval queue (demo mode, owner approval required); never executes. Old POST /api/tradingview now shares the secret helper and _store_tradingview_alert (same behaviour). Live-verified: wrong secret 403 on both, non-JSON 400, valid alert dry run (rejected by model_alignment), HOLD rejected, dry_run false forced to dry run, approval queue unchanged, 3 journal rows; deep doctor healthy, 108 tests. Three verification alerts ("SAFE-UPGRADE VERIFICATION - not a signal") are in the TradingView alert history. Live dir is not a git repo: no commit (backup app.py.bak.*_webhook_tradingview).
- 2026-09-14: BTCUSD swap measured (MT5 tester copy, BTCUSD H4 2025-26): -0.056% of price per night, every calendar night, 1 BTC/lot. strategy_lab: ROLLOVER_CALENDARS {"forex": Wed x3 weekdays, "crypto": every night}, rollover_counts(times, weekday_weights), HOLDING_COSTS BTCUSD (calendar crypto), Market picks the calendar per symbol; test_crypto_calendar_charges_every_night_and_btc_uses_it (23 lab/swap/book tests pass). Rescored BTC 4h/1h/1d (2,841 candidates) and updated the book for BTC markets (1h: 4 demoted). No market is spread-only any more. AI employee has 10 pending proposals awaiting the owner.
- 2026-09-14: TradingView daily plan finished inside the system. src/tradingview_plan.py (build_daily_plan, follow_trades, save_daily_plan, pine_script_text) reuses strategy_lab.strategy_orders + new shared strategy_lab.ema_pullback_conditions (_signals_ema_pullback now calls it; identical signals, tested) and daily_report.analyse_levels/analyse_momentum; nearest support/resistance re-picked from the latest H4 close (analyse_levels measures from the daily close, which put a support above price). API GET /api/tradingview/plan?symbol= (5-min cache, saves data/tradingview_plans/<date>_<symbol>.json + latest_<symbol>.json), GET /api/tradingview/indicator (Pine text). /tradingview page: "Daily plan" card with lightweight-charts broker H4 candles + price lines (entry/SL/TP, nearest support/resistance, prev day H/L), plan table, checklist, "Copy TradingView indicator" button and paste steps. tests/test_tradingview_plan.py (3) in doctor TEST_FILES. Checked the user's Edge: 23 processes, none with remote debugging, no DevTools port, so Claude cannot draw on their logged-in TradingView; the user pastes the indicator.
- 2026-09-14: /tradingview (live app.py): Full page buttons for the Daily plan card and the Live charts card (Esc closes, ?full=plan|chart deep link); Daily plan chart is now live (GET /api/tradingview/live-price = MT5 bid/ask, polled every 3 s, moves the forming H4 candle, reloads plan on new candle) and has a TradingView live (4H widget) toggle (?plan_chart=tv). app.py has no debug reloader in practice: template/route edits need the app process restarted. Backup app.py.bak_20260914-105827_tvfullpage.
- 2026-09-14: /tradingview entry popups: page checks both Daily plans every 60 s; status new_setup pops a toast once per setup (localStorage key symbol|candle|entry) with beep and optional Windows notification (Desktop alerts button); Test entry popup button and ?entry_test=1. Alerts only, no orders; works only while the page is open. Backup app.py.bak_20260914-113250_tvpopup.
- 2026-09-14: Daily self-learning scheduled (user requirement): task "SmartEntry Daily Learning" 05:30 -> scripts/run_daily_learning.cmd -> python -m src.daily_learning --symbols XAUUSD BTCUSD (new CLI main, gate on, log data/learning/daily_learning.log, decisions data/learning_decisions.json). Added to system_doctor TASKS (auto-recreate) and daily_report SCHEDULE. App load_model reads from disk per call, so promoted models go live without restart.
- 2026-09-14: /self-learning page (nav AI group) + GET /api/learning-curve from new src/learning_curve.py (trend = last 7 runs vs 7 before, +-1 pt; task stale after 30 h; gate money test; Strategy Lab totals; paper trader). Tests tests/test_learning_curve.py (5) added to system_doctor TEST_FILES. Daily history cap raised 52 -> 1000. First verdict: gold RF worse (-2.3 pts), BTC RF flat; live models still lose on unseen bars. Backup app.py.bak_20260914-121027_selflearning.
- 2026-09-14: Self Learning verdict made honest (user: 'i want to know the true'): one run per day, change must beat the 95% chance band of one run (~4.6 pts at 450 test bars) else 'no_real_change', checked same-bar old-vs-new runs counted (10 needed). The earlier 'Getting worse' (-2.3 pts) was noise: one 12 Aug run + 4 identical 19 Jul repeats + different test months. Tests 7. Backup app.py.bak_20260914-122519_honestcurve.
- 2026-09-14: Obsidian connected: installed via winget (AppData\Local\Programs\Obsidian), vault Documents\SmartEntry Vault registered in %APPDATA%\obsidian\obsidian.json. src/obsidian_notes.py writes Home, Daily/<date>, Learning/<date> Learning + Learning Curve, Research/Research Log (BASELINE rows), Trades/Trade Journal + one note per paper trade/TradingView alert (write-once). Generated notes keep '## My notes'; removing generated_by marker stops updates. Task 'SmartEntry Obsidian Notes' hourly :50 (system_doctor TASKS + daily_report SCHEDULE). learning_curve.paper_state_path shared. Tests 4 (tests/test_obsidian_notes.py).
- 2026-09-14: Daily Report gold sessions were empty on Mondays (AI employee proposal c9b25d0a4c 'stale XAUUSD feed'): analyse_sessions took the last calendar UTC day, Sunday, when gold only has 22-23 UTC bars. Now uses the latest completed day with a bar in every session (falls back to the last day). Test test_sessions_skip_a_closed_market_day; 7 daily report tests pass; report regenerated (XAUUSD sessions 2026-09-11, BTCUSD 2026-09-13). Backup _backups/daily_report.py.bak_*_sessions.
- 2026-09-14: AI employee proposals one by one (user: 'One by one safer option always'). #11 test-count jump 102->112: verified intentional (new test files that day), closed done; doctor suite now 120. #8 'dry_run override': rejected, premise wrong (places_orders is only a read-only reply label; the single dry_run event was the 13 Sep manual check). #12: src/demo_executor.sync_positions now journals dry_run_expired once per dry-run attempt past expires_at (no broker calls; test added; 23 demo/paper tests pass); app restarted via keep-alive (PID 58956), live sync logged it for bar 2026-09-13 08:23; doctor healthy. Backup _backups/demo_executor.py.bak_20260914-143200_dryrunexpiry.
- 2026-09-14: AI employee #1 RAM trend done (monitoring only): system_doctor history rows store free_ram_mb + app_pid; apply_ram_trend (6 h window, same app PID, drop >= 500 MB -> PC resources info, never warn; <500 MB warn unchanged). test_ram_trend_flags_a_sharp_drop_only_within_the_same_app_process; 9 doctor tests pass; live row verified. Backup _backups/system_doctor.py.bak_20260914-143507_ramtrend.
- 2026-09-14: claude-mem plugin (user installed; asked for a skills check): 20 skills load, but its worker never started - log 'Bun runtime not found' (worker needs bun:sqlite). Installed Bun 1.4.2 via winget (Oven-sh.Bun, user PATH updated; this shell needs the WinGet Packages bun-windows-x64 path until restart). Started worker: node scripts/bun-runner.js scripts/worker-service.cjs start -> /api/health ok on 127.0.0.1:37777. Future sessions start it from the SessionStart hook.
- 2026-09-14: AI employee #10 done (monitoring only): src/strategy_book.py market_leaders / record_leaders / leader_trend; update_book appends book.leader_history (1000 rows) in its finally block (errors go to report problems, never block the save); book_summary gains leader_trend (latest DSR, change since previous, leader_changed, best_ever, 48-point series). test_leader_history_follows_the_best_deflated_sharpe_per_market; 24 book/lab/swap tests pass. Live read-only leaders: XAUUSD:4h 0.7525, XAUUSD:1d 0.6913, XAUUSD:15m 0.069, XAUUSD:1h 0.032, BTCUSD:1h 0.080, BTCUSD:4h 0.066 (none near 0.95). App not restarted for this: /api/strategy-lab shows leader_trend after the next app restart; hourly job records rows regardless. Backup _backups/strategy_book.py.bak_20260914-144054_dsrtrend.
- 2026-09-14: AI employee #9 BTCUSD 15m zero validated (read-only diagnosis, no change): costs, not scoring or history. Validation SR>0 in 1.3% of 2,701 (XAU 15m 53.5%, BTC 1h 43.2%); last 300 rejections 292 lose in search / 294 in validation, 15-24 short of trades; median search PF 0.63; BTC round trip 0.12% = 0.53x median 15m bar range (gold 15m 0.29x). Gates unchanged; stopping BTC 15m search left to the owner.
- 2026-09-14: AI employee #6 dedupe done (archive, never delete): src/strategy_book.py _result_signature + dedupe_watchlist (identical search+validation+holdout trades/PF/return/DD within market+family; keeper = book rank, then earliest first_added; approved never archived; archived_reason + duplicate_of) runs before cap_watchlist in update_book (counts duplicates_archived) and via 'python -m src.strategy_book dedupe' (lab lock). Test test_dedupe_archives_identical_results_and_keeps_the_best_copy; 25 book/lab/swap tests pass. Live: 22 archived, watchlist 60 -> 38, 0 identical groups, leaders unchanged (XAUUSD 4h DSR 0.7525). Markets may now hold < 10 watchlist entries; archived ones are never re-added. Backups _backups/strategy_book.{py,json}.bak_20260914-145856_dedupe.
- 2026-09-14: Owner approved two standing rules (AI employee #2 c2d2f586fb, #7 1d4bbf5364), recorded only, nothing changed: (1) the 0.95 deflated Sharpe holdout bar is never lowered; XAUUSD 4h candidates keep being watched hourly with the DSR trend; (2) no live SwingTrendPullback input/preset change until a book candidate clears DSR 0.95 plus the other holdout criteria (research, backtests, tester-copy runs and review presets still allowed). Added to C:\Users\th_em\CLAUDE.md evidence rules.
- 2026-09-14: Economic calendar connected (user: 'System need to connect to economic calendar forex' + forexfactory.com/calendar). src/economic_calendar.py (reuses walkforward_backtest._iso after dup hook): ForexFactory weekly export nfs.faireconomy.media/ff_calendar_thisweek.json (page is behind Cloudflare), download at most hourly, cache data/economic_calendar/thisweek.json, keeps last good copy on failure (stale > 24 h flagged), available:false when no data (never invents), times to UTC, RELEVANT_CURRENCIES XAUUSD/BTCUSD -> USD+All, news_window 30 min before/after High impact, CLI 'python -m src.economic_calendar [--refresh]'. Information only, no trading path reads it. app.py: GET /api/economic-calendar (?refresh=1 local only); /api/jarvis/market/economic-calendar now serves it (the hard-coded fake jobless-claims sample in jarvis_realtime_market_data is no longer served; file untouched). Daily Report: economic_calendar_section (injectable, offline test uses a fake) + page card. Tests tests/test_economic_calendar.py (5) added to system_doctor TEST_FILES; daily report/calendar/doctor 21 pass. App restarted (PID 59260), both endpoints verified live (16 High / 10 Medium / 77 Low this week; next high impact Fed Funds Rate 2026-09-16 18:00 UTC). Backups app.py/system_doctor.py/daily_report.py .bak_20260914-152912/152943_econcalendar. AI employee proposal #5 closed done.
- 2026-09-14: Migration "make ml_trading_system the live system" (safe-upgrade, owner chose: copy live data in). DONE so far (nothing in C:\Users\th_em deleted or edited): snapshot code backed up to _backups/snapshot_code_20260914 (162 files); June data/models/logs renamed to data_snapshot_20260630 / models_snapshot_20260630 / logs_snapshot_20260630 (its auto_trader_state had autonomy+auto_execute+session ON - must never be restored as live state); 144 app code files copied from home (app.py, jarvis_*.py/json, telegram_config.py, src ai voice trading memory tests scripts static strategies config dashboard windows) - byte-identical check 0 missing/0 different; scripts\*.cmd (8) and start_trading.bat repointed to cd /d C:\Users\th_em\ml_trading_system; .gitignore: data/ models/ logs/ *_snapshot_*/ _backups/ telegram_config.py *.log; 129 tests pass here; committed on branch claude/make-ml-trading-system-live (runtime data, logs and secrets unstaged/ignored). NOT DONE: the live switch (pausing tasks, stopping the home loop/app, copying data/models/logs, repointing the 8 tasks and the Startup shortcut, starting the new loop) was refused by the Claude Code auto-mode permission classifier ("interfere with workloads"). The live app still runs from C:\Users\th_em, all 8 tasks still point home. Owner runs the switch: powershell -NoProfile -ExecutionPolicy Bypass -File C:\Users\th_em\ml_trading_system\scripts\switch_live_to_ml_trading_system.ps1 (waits for jobs, refuses if a demo model position is open, pauses tasks, stops home loop then app.py (verified), robocopy /E data models logs, repoints tasks + shortcut, starts new loop, runs system_doctor).
- 2026-09-14: ROLLBACK (after a switch): powershell -NoProfile -ExecutionPolicy Bypass -File C:\Users\th_em\ml_trading_system\scripts\rollback_live_to_home.ps1 [-CopyDataBack]. Manual equivalent: 1) schtasks /change /tn "<each SmartEntry task>" /disable; 2) stop the cmd.exe running ml_trading_system\start_trading.bat, then the python app.py on :5000; 3) optional: robocopy C:\Users\th_em\ml_trading_system\data C:\Users\th_em\data /E /XO (same for models, logs) to keep state written after the switch; 4) schtasks /change /tn "<task>" /tr C:\Users\th_em\scripts\<script>.cmd then /enable for all 8 (Paper Trader run_paper_trader, Strategy Lab run_strategy_lab, System Doctor run_system_doctor, System Doctor Daily run_system_doctor_deep, Daily Report run_daily_report, AI Employee run_ai_employee, Daily Learning run_daily_learning, Obsidian Notes run_obsidian_notes); 5) Startup shortcut SmartEntryProAI.lnk -> C:\Users\th_em\start_trading.bat, working dir C:\Users\th_em; 6) start C:\Users\th_em\start_trading.bat; 7) netstat -ano | findstr ":5000" shows one LISTENING PID; python -m src.system_doctor healthy. Home copy was left untouched as the fallback.
- 2026-09-14 ~16:05-16:10: the owner's first run of scripts\switch_live_to_ml_trading_system.ps1 stopped at Windows password prompts from `schtasks /change`, after it had stopped the home keep-alive loop + app.py and copied data/models/logs into ml_trading_system. Result: dashboard down ~5 min (tasks and Startup shortcut still pointed home; autonomy off, no demo position open). Restored by starting C:\Users\th_em\start_trading.bat (app PID 54472; doctor healthy, MT4 12755139 + MT5 connected). The partial copy in ml_trading_system\data is refreshed by the next switch run (robocopy /E).
- 2026-09-14: No-password task handling (owner request). Switch and rollback scripts: Disable-/Enable-ScheduledTask instead of schtasks /change; repoint = Export-ScheduledTask XML -> change only <Command> -> principal UserId = current user SID, LogonType InteractiveToken, RunLevel LeastPrivilege -> Register-ScheduledTask -Xml -Force (same name, triggers, settings) -> verified (Execute, Interactive, Limited). Scripts are re-runnable after a partial run. System Doctor (ml_trading_system copy only): fix_missing_tasks now runs task_register_command = PowerShell New-ScheduledTaskAction/Trigger (task_trigger_ps maps the TASKS schtasks-style schedule: hourly/minute -> -Once -At -RepetitionInterval, daily -> -Daily -At) + New-ScheduledTaskPrincipal -LogonType Interactive -RunLevel Limited + Register-ScheduledTask -Force; TASKS "schedule" key kept. Test updated. Home copy untouched.
- 2026-09-14: ForexFactory MCP server for Claude (owner: find the forex economic calendar MCP, "best option safe"). Chosen: github.com/kjpou1/forexfactory-mcp (MIT) at commit 97c77689e14718f390efcbbfe4648449536b4f43, cloned to C:\Users\th_em\mcp-servers\forexfactory-mcp. Source reviewed before install: only contacts https://www.forexfactory.com (Playwright headless Chromium reads window.calendarComponentStates), no other URLs/subprocess/eval/telemetry, stdio transport (no port). Local patch: removed Chromium "--no-sandbox" (sandbox stays on; still works). Installed uv 0.12.13 (winget astral-sh.uv); `uv sync --frozen` made an isolated .venv (Python 3.14.7) - trading app Python 3.10 untouched; Playwright Chromium in %LOCALAPPDATA%\ms-playwright (656 MB). Registered: `claude mcp add --scope user forexfactory -- C:\Users\th_em\mcp-servers\forexfactory-mcp\.venv\Scripts\python.exe -m forexfactory_mcp.server` (user config .claude.json). Tool ffcal_get_calendar_events(time_period today|tomorrow|yesterday|this_week|next_week|last_week|this_month|next_month|last_month|custom, start_date, end_date) -> id, title, currency, impact, datetime_utc/local, actual, forecast, previous. Smoke test mcp-servers\forexfactory-mcp\test_this_week.py. Remove: `claude mcp remove forexfactory --scope user`. Dashboard /economic-calendar page work paused (app.py nav entry + Daily Report link added, not live until restart; backup _backups/app.py.bak_20260914-162711_econpage).
- 2026-09-14: Dashboard Economic Calendar page finished (owner: "that is not really the economy calendar from forex", then "finish"). app.py: ECONOMIC_CALENDAR_TEMPLATE + GET /economic-calendar (Data nav entry; Daily Report calendar card links to it): whole week, every currency from the ForexFactory export (/api/economic-calendar all_events), grouped by day in the viewer's local time, currency and impact filter chips (saved in localStorage), colour impact bars, past rows dimmed, next event highlighted with countdown, tiles (next high impact, gold news window, week counts, data freshness), refresh button (local ?refresh=1), note that the export has no Actual column with link to forexfactory.com/calendar. Information only. App restarted twice (PID 32288); doctor: MT4 12755139 + MT5 connected, autonomy off; Edge screenshots desktop + phone checked; link colour fixed (.container a #7dd3fc). Backup _backups/app.py.bak_20260914-162711_econpage. Actual values for Claude come from the forexfactory MCP server (see earlier note).
- 2026-09-14: Doctor warn "SmartEntry Strategy Lab last result 1" after the 16:20 slot: the log shows a normal run started 16:19:09 local (search 1,000 candidates in 2.8 min, book update ok at 16:22), while Task Scheduler records a start at 16:20:01 with result 1 and no log lines -> most likely a second, overlapping start that could not append to the locked log. Task Scheduler operational log is disabled, so the 16:19 starter is unproven. No code change; check the 17:20 run result before acting.
- 2026-09-14 17:15: Resumed after restart. ForexFactory MCP works in Claude (today's events with Actual values). Bun/uv are in the user PATH but this Claude session inherited the old PATH, so the claude-mem worker (restarted, pid 50588) still reports uvx missing (vector search off) - fixed by opening Claude Code from a fresh terminal. Switch script hardened: aborts (re-enables tasks, app untouched) if src jobs still run after 15 min instead of copying anyway; switch and rollback now start the keep-alive loop via explorer.exe + Startup shortcut (detached from the calling shell). Both parse clean; doctor tests 9 pass. Claude's run of the switch was refused again by the auto-mode classifier ("Production Deploy"): live app still C:\Users\th_em (PID 32288), tasks still home. Strategy Lab "last result 1" (16:20) still to re-check after the 17:20 run.
- 2026-09-14 ~17:45: TradingView (user's Chrome, account themiskolias45, Basic plan): Paper Trading broker connected with the user's approval (balance 99,986.10 USD, no positions). User waived approval for Paper Trading orders only (memory browser-action-approval-rules). User pasted and asked to use their Pine "Swing Trend Pullback Strategy (21/50 EMA) - Long Only" (= strategy_lab spec as_coded; our backtest after spread+swap fails: gold 4h holdout PF 1.09 +4.1% DD 23.9% DSR 0.44, BTC 4h PF 1.04 -0.4%; rows in BASELINE.md). Added to the XAUUSD 4h OANDA chart as an UNSAVED script (Basic plan allows 1 saved script; upgrade not clicked) - it can disappear if the layout reloads. TradingView tester on it: 2023-01-02 to 2026-09-14 +3.84%, max DD 2.40% (10% equity sizing, no swap). Session /loop now follows its dashboard on TradingView Paper Trading (1 unit, SL = dashboard SL trailing, TP = TP2; only while this Claude session runs). Live EA inputs unchanged.
- 2026-09-14 ~17:55: Tested both SwingTrendPullback versions on 1h/4h/1d XAUUSD+BTCUSD (spread+swap): both fail everywhere, live EA inputs better than the pasted 21/50 on every market (rows in BASELINE.md). Best book candidate XAUUSD 4h 40-bar breakout + EMA200 (DSR 0.73, holdout PF 2.94, 47 trades). Offered it for the paper test; owner chose to keep paper-testing their pasted 21/50 script ("the strategy is profitable"). Paper watch unchanged.
- 2026-09-14 ~18:00: Owner asked to make the pasted 21/50 STP more profitable. Two honest variant rounds (scratch scripts improve_stp.py / improve_stp2.py, lab registry untouched): 282 variants per market on XAUUSD 4h and 1d, 0 pass search+validation gates, holdout never opened. Best tweak: stop 2 ATR + 3R target (4h search PF 0.85 -> 0.92, still -20% 2008-2022). Stopped tuning to avoid overfitting. Paper watch still follows the owner's original script on TradingView. Strategy Lab 17:20 task result 0 (16:20 "result 1" was a one-off).
- 2026-09-14 ~18:45: Owner: "is same, not been any improvement". Built Pine v2 (scratchpad swing_trend_pullback_v2.pine): stop 2.0 ATR + 3R target (best tested tweak; broker backtest 2008-2026 gold 4h total -15.2% -> -1.7%, BTC 4h -27.9% -> -1.7%; did NOT pass holdout/DSR) + fixes (swing low every bar, stop/target sent with the entry, entry = real fill, ratcheting trail, dashboard on last bar only, title "Swing Trend Pullback v2 (21/50 EMA, 2ATR / 3R) - Long Only", table "Status v2"). Replaced the original on the TradingView chart (same unsaved Untitled script), compiled without errors; TV tester 2023-01-02..2026-09-14 +4.37% DD 2.75% (original +3.84% DD 2.40%). Layout saved. Paper watch loop now follows v2. Live EA unchanged.
- 2026-09-14 ~18:50: TradingView Paper Trading had disconnected after the v2 update/layout save; reconnected with the owner's approval (Trade > Paper Trading only). No orders placed.
- 2026-09-14 ~19:05: Round 3 (owner: "work bit more") on v2: shorts and EMA200 filter hurt gold; stop/target neighbours confirm 3-3.5R target beats 2-2.5R but none fixes 2008-2022 losses; BTC best gate-passer failed its holdout (PF 0.84, -14.8%). No change to v2 on the chart (switching to a neighbour picked from all-years totals would use holdout years). Rows in BASELINE.md.
- 2026-09-14 ~19:20: Round 4 daily trend filter on v2: gold 0/3 pass (D1 close>EMA50 halves the 2008-2022 loss but also halves recent profit); BTC D1 close>EMA50 passed gates then failed holdout (PF 0.89, -8.3%). v2 on the chart unchanged. Four rounds, ~300 variants: this pullback family has no robust edge after costs.
- 2026-09-14 ~19:40: MT5 ATOMIC improvement (owner: "only mt5 atomic improvement", with photos of the MT4 Atomic Analyst inputs/chart/panel). MT4 = paid Market "Atomic Analyst" (.ex4, closed) wrapped by a V84 panel; MT5 had only ATOMIC_ANALYST_V84.mq5 (panel + JSON feed, no chart features, reads bar 0). New ATOMIC_ANALYST_V85.mq5 in MT5 D0E8 MQL5\Indicators (V84 untouched), compiled 0 errors / 0 warnings. Own documented engine (not the MT4 formula): ATR trailing stop (strength period 17, 1.5 ATR) flip confirmed by smoothed momentum, optional AI filter (RSI+MACD) and Atomic filter (MA 250); closed bars only (no repaint). Features: colour candles, arrows (Wingdings 217/218, EA buffers 0 buy / 1 sell), trailing line + flip dots, EMA 21/50 lines, SL + TP1-5 (0.618/1/1.618/2/2.618 x risk) lines with Fibo labels for last N signals, trade analysis lines + profit labels in points, on-chart stats (spread deducted, no swap, not evidence), alerts popup/push/email/sound once per closed-bar signal, CSV signal log, panel (MTF 9 TFs, consensus 7+final, decision = consensus AND atomic trend, dominance of 17 votes shown as "Agreement", evidence matrix 8 rows, signal ticket with status, statistics + account day/week/total profit, status bar, gradient, theme, minimise/close/restore buttons, drag), JSON feed same path atomic_analyst\<SYMBOL>.json schema 2 (superset of V84; no reader found in the live app). Needs the owner to attach it in MT5 (GUI) and remove V84 from that chart so both do not write the same feed file.
- 2026-09-14 ~19:55: ATOMIC V85 fixes after the owner's chart photos: readable cells (darker fills, bold 8pt, dark text on light fills), MT5 arrow codes 233/234 (217/218 draw diamonds in MT5), trade closes on trailing-stop flip (result 4, input InpExitOnTrailFlip, fixes "open SELL while trend BULL"), shorter stats text. Owner: status buttons did not work -> all 7 status-bar cells are now clickable buttons: SESSION = session filter 07-21 GMT (recalculates), SPREAD = spread guard for alerts, CONNECTED = refresh, AI FILTER / ATOMIC = filters (recalculate via ChartSetSymbolPeriod), GRADIENT = gradient, ALERTS = master alerts switch. Compiled 0/0.
- 2026-09-14 ~20:10: ATOMIC V85 round 3 (owner photo on BTCUSD H1 "something not correct", then "flashing" on panel and chart). Causes and fixes: panel deleted/recreated ~250 objects every 5 s -> RebuildPanel(full) only on layout changes, periodic refresh updates in place; trade objects deleted/recreated each new bar -> in-place update + stale-name cleanup. Max spread default 999 pt made BTC (~1,700 pt normal) always "SPREAD HIGH" and blocked alerts -> InpMaxSpread 0 = auto (spread <= 15% of ATR). Ticket showed a stopped-out SELL as "Active" under BUY ONLY -> title "Last Signal (closed) - no active trade", grey direction with its R result. Points were meaningless on BTC (+860824) -> R multiples on trade labels and "Total result +xR | price". Decision box now says why a verdict has no arrow yet.
- 2026-09-14 ~20:30: Owner asked for the best combination of ATOMIC + system + Swing Trend Pullback. Backtested pre-stated combos (scratch combo_atomic_stp.py) on XAUUSD 4h/1h and BTCUSD 1h: no Atomic-based combination passes; Atomic logic alone loses badly on BTC 1h and fails validation on gold; adding the Atomic trend/AI filter to STP v2 does not fix STP's 2008-2022 losses. Only positive result: portfolio STP v2 + book breakout on gold 4h (lower drawdown than STP alone), but the breakout alone is better and that portfolio number is holdout-biased. Rows in BASELINE.md.
- 2026-09-14 ~20:45: Owner: "can you improve the atomic". Tested 8 pre-stated V85 signal versions (scratch improve_atomic.py) on XAUUSD 1h/4h and BTCUSD 1h: 0 pass anywhere; higher-TF agreement + wider trail (+ ADX) cut losses a lot on gold 1h and BTC 1h but stay negative; on gold 4h all changes were worse. V85 left unchanged; offered optional HTF/ADX filter toggles as loss reducers (not edge).
- 2026-09-14 18:44 UTC: paper watch check: v2 FLAT, no paper position, no news window. v2 "Status v2" table was not rendering (cells only drawn on barstate.islast); changed to update every bar like the original, re-pasted into the unsaved Untitled script, updated on chart, layout saved; table now shows Status v2 FLAT. TradingView tester on v2 now 2023-01-02..2026-09-14: 101 trades, 47.5% win, PF 1.70, +5.36%, DD 1.78% (differs from the earlier 106 trades / +4.37% - the report was refreshed on newer bars). Paper Trading still connected.
- 2026-09-14 ~21:00: ATOMIC V85 loss-reducer switches added (owner "ok"), all OFF by default, inputs + panel buttons row under the status bar: HTF TREND (arrow must agree with the higher-TF Atomic trend from the last CLOSED HTF bar; H1->H4, H4->D1, M15->H1...), ADX>20 (iADX 14), LONG ONLY, WIDE TRAIL 2.5 ATR. Each toggle recalculates arrows/stats. Panel note: fewer bad H1 arrows, not proof of profit, leave OFF on H4 (backtest rows in BASELINE.md).
- 2026-09-14 ~21:10: ATOMIC V85: owner photo showed the new loss-reducer row loaded; SL/TP/Fibo/R text labels drew over the panel (OBJ_TEXT foreground) -> OBJPROP_BACK true; switch note shortened (was cut off).
- 2026-09-14 ~21:20: Owner changed TradingView v2 inputs (slow EMA 52, slope lookback 9, pullback tol 0.65) and says it is more profitable. Broker backtest: better 2023-26 (holdout +26.4% vs +18.1%, DD 15.7% vs 22.6%) but worse 2008-2022 (-41.6% vs -30.2%), all years -8.9% vs -1.7%. Neighbour grid: slope 9-12 helps recent years robustly; EMA 52 hurts history in every cell; EMA 50 + slope 3-12 all years +2..+12%. Paper watch follows whatever inputs are on the chart. Live MT5 EA inputs unchanged (owner rule).
- 2026-09-14 ~21:30: Owner set TradingView v2 slow EMA back to 50 (now EMA 21/50, slope lookback 9, pullback tol 0.65, stop 2 ATR, 3R). Broker backtest (holdout-biased pick): all years about +2%, 2023-26 about +53%. Paper watch follows these chart inputs from now on.
- 2026-09-14 ~21:00: Daily trading agent built (owner: research -> trade -> journal like an Alpaca agent, for the TradingView daily plan; chose the SYSTEM PAPER LEDGER and watchlist XAUUSD + BTCUSD). src/daily_agent.py (live dir): research = broker candles via the app (mtf_data source=app, no cache), tradingview_plan.build_daily_plan with AGENT_SPEC = owner TradingView v2 inputs (EMA 21/50, slope 9, tol 0.65, push 15>0.6, bull close, RSI>40, ATR14; swing stop 2 ATR, 3R, trail 3.5, 150 bars), economic calendar news window + next 24h high impact, ATOMIC V85 feed (evidence only, <3 h old). Decision once per closed H4 candle per symbol: BUY (fresh setup, fill at next H4 open, 1% of 10,000 USD ledger at risk), HOLD, CLOSE (stop/trailing/target/time, spread + swap charged), WAIT, NO_TRADE, SKIP_NEWS, NO_DATA; every decision journalled with reasons in data/daily_agent/journal.jsonl + journal/<date>.md + Obsidian vault Agent/<date> Agent Journal.md; ledger data/daily_agent/state.json; plans data/daily_agent/plans. No live mode, no broker imports (test enforces). Tests tests/test_daily_agent.py (7) in doctor TEST_FILES. Task "SmartEntry Daily Agent" hourly :10 (PowerShell Register-ScheduledTask, interactive, limited) + doctor TASKS + daily_report SCHEDULE. Page /daily-agent (Automation nav) + GET /api/daily-agent. app.py backup _backups/app.py.bak_20260914-205133_dailyagent; app restarted via keep-alive (PID 3504, one listener). First run 19:52 UTC: XAUUSD NO_TRADE, BTCUSD NO_TRADE (candle 13:00 UTC; broker H4 candles close 01/05/09/13/17/21 UTC).
- 2026-09-15: CRT_Dashboard_EA (MT4, IC Markets demo 12755139) reviewed read-only and backtested (user asked for the "best safe option"; live EA untouched). New src/crt_lab.py (`python -m src.crt_lab run`) registers family "crt" via new strategy_lab.ORDER_BUILDERS; strategy_lab.simulate_orders gained optional be_trigger_r/be_lock_price/trail_start_price/trail_dist_price/trail_start_r/trail_dist_r (absent = unchanged; 29 tests pass incl. new tests/test_crt_lab.py). Backup src/strategy_lab.py.bak_20260915-135916_crt. Report data/strategy_lab/crt_backtest_20260915.json. Verdict: no variant passes; the live $-fixed trailing stop is the worst exit on both symbols. IC Markets MT4 server = New York + 7 h.
- 2026-09-15: CRT round 2 (`python -m src.crt_lab run --round 2`, report data/strategy_lab/crt_backtest_round2_20260915.json, filter_mask + tests): no pass. Gold watchlist only = M15 EMA50 agrees + fixed 2R (holdout PF 1.39, DSR 0.49); BTC 15m CRT dead after costs. The holdout is now spent for CRT: next evidence must be forward (paper) bars, not more variants on the same holdout.
- 2026-09-15: CRT + FVG retest and CRT AMD tested (src/crt_fvg_lab.py, strategies/crt_fvg_amd.md, tests/test_crt_fvg_lab.py; strategy_lab.simulate_orders gained optional entry_prices for resting limit orders, Market.simulate accepts 4-array builders). 64 variants on gold+BTC 15m: 0 pass even search+validation. Report data/strategy_lab/crt_fvg_backtest_20260915.json. Only surviving CRT idea remains gold EA entry + M15 EMA50 + fixed 2R (watchlist, needs forward bars).
- 2026-09-15: Gold CRT paper forward test started (user "next"): src/crt_forward.py (`python -m src.crt_forward [--status]`), candidate = CRT_Dashboard_EA entry + M15 EMA50 agrees + fixed 2R, fresh MT5 bars via the app each run, start_at 2026-09-15 13:34 UTC fixed, closed trades kept across windows, verdict collecting until 30 trades (PF >= 1.2, positive, DD <= 20%). State data/strategy_lab/crt_forward.json, log crt_forward.log, task "SmartEntry CRT Forward" hourly :25 (scripts/run_crt_forward.cmd, in system_doctor TASKS + TEST_FILES). Never places orders.
- 2026-09-15: Gold CRT candidate FAILED on never-used older bars (Vantage MT5 M15 2022-06 -> 2024-08, the only older history the terminal serves; cache data/research/cache/xauusd_15m_mt5hist_2018_2024.csv + 4h): 98 trades PF 0.85 -10.6R net; filter no better than no filter. Forward test task left running (harmless, never trades) but expectation is low; CRT gold 15m research should stop unless the owner wants a different idea tested.
- 2026-09-15: CRT higher timeframes (src/crt_htf_lab.py, tests/test_crt_htf_lab.py, report data/strategy_lab/crt_htf_backtest_20260915.json): 0/48 pass on gold/BTC 1h/4h/1d. With the 15m rounds (104 per market) and the unseen 2022-24 gold test, CRT is exhausted as a stand-alone rule; do not reopen without a genuinely new idea.
- 2026-09-15: Owner's CRT pictures (sweep -> MSS -> FVG 50%) tested: src/crt_mss_lab.py, tests/test_crt_mss_lab.py, report data/strategy_lab/crt_mss_backtest_20260915.json. FAIL (best gold 5m previous-day range: passed search/val, holdout -5.5R net). Also fixed mtf_data.fetch_mt5_bars server-time bug (MT5 bar times are UTC+3 in summer, not UTC) and added 5m to mtf_data.TIMEFRAMES and strategy_lab.TIMEFRAME_MINUTES; reran the unseen gold CRT test (still fails).
- 2026-09-15: crt_forward now tracks two gold candidates (CANDIDATES in src/crt_forward.py): crt_ea_ema50_15m (app 15m, crt_forward.json) and crt_mss_d1_5m (owner's picture model, previous-day range, MT5 5m via fixed fetch_mt5_bars, crt_forward_mss_d1_5m.json). Same hourly task; one candidate failing does not stop the other. Both carry weak or failed backtest evidence: forward bars are the only remaining test.
- 2026-09-15: SwingTrendPullback swap study (src/stp_swap_lab.py, tests/test_stp_swap_lab.py, report data/strategy_lab/stp_swap_backtest_20260915.json): exiting before the Wednesday triple rollover / every rollover / after 1 day does not improve search+validation for live inputs, steady or trend rider; nothing to propose for the live EA. Live EA (magic 996611, Vantage demo 25446287) has 0 trades since 13 Sep (gold below EMAs). Strategy book already forward-rechecks watchlist hourly.
- 2026-09-15: Signal parity (safe-upgrade): src/signal_engine.py is now the single place for RF/LSTM weights and BUY/SELL thresholds (config "live" 50/50 >= 0.55 / <= 0.45; "legacy_ensemble" 60/40 strict for ensemble_predict). app.py live payload, _classify_signal, shadow blend, train.ensemble_predict and walkforward_backtest go through it; backtest_signal_series = evaluation entry. tests/test_signal_parity.py in System Doctor deep (162 pass). Entry-reviewer map found, NOT changed (no order-path edits): build_features dropna on the 3-bar future target makes live features.iloc[-1] 3 bars old; walk-forward is RF-only (live ensemble never backtested); live SL/TP from Yahoo GC=F; auto_trade_execute_api has no spread/news/freshness check and trusts internal_auto_execute from the POST body; approved TradingView requests execute the model side, not the approved side; simulate_demo_trade outcomes are synthetic and feed the autonomy memory shift.
- 2026-09-15: Execution safety 1 (safe-upgrade, dry run unchanged): /api/auto-trade/execute now requires X-Control-Secret (data/control_api.json, created once; injected into /auto-trader only for 127.0.0.1 requests, other devices paste it into localStorage 'smartentry_control_secret'); internal_auto_execute is never read from a request (autonomy + JARVIS voice call _logged_execute in-process; voice approval also needs this PC or the secret). New src/execution_guard.py pre-order checks before any order or simulation: fresh closed MT5 bar <= 75 min, spread <= 25% of stop, no high-impact news window (src/economic_calendar); missing data rejects. Every rejection (HTTP >= 400) -> data/execution_rejections.jsonl (first 7 rows 18:25-18:34 UTC are from test runs before the test path fix). Tests test_execution_guard (5), test_execute_api_security (4); System Doctor deep 171 pass. Backup app.py.bak_20260915-190847_execsafety1.
- 2026-09-15: Execution safety 2: orders' entry/SL/TP now come only from the broker that fills them - execution_guard.broker_trade_levels (entry = MT5/MT4 ask for BUY, bid for SELL; ATR14 from the same broker's 1h bars; 1.2/2.4/3.6/4.8 ATR as the live plan) + levels_share_source; no Yahoo fallback. MT4 bridge has quotes but no bars, so MT4-platform orders are refused (levels unavailable) rather than mixed. trade_record gains price_sources + display_plan_entry. build_live_plan (Yahoo) kept for the dashboard only. tests/test_broker_levels.py (3); System Doctor deep 174 pass. Backup app.py.bak_20260915-193524_execsafety2.
- 2026-09-15: Execution safety item 3 (TradingView approval exact match): src/execution_guard.approval_price_band/approval_matches; app._approval_price_band_now stores an MT5 broker price band (entry +/- 0.3 x stop distance) on every new approval request; /api/auto-trade/execute with approval_request_id rejects (409, logged, request.last_mismatch saved) when symbol, side, feed or broker entry differ from the approved band, and legacy requests without a band are rejected. tests/test_approval_match.py (3) added to system_doctor TEST_FILES; System Doctor deep 177 passed. Dry run unchanged. Backup app.py.bak_20260915-194617_execsafety3.
- 2026-09-15: Execution safety item 4 (live signal freshness): build_features dropped the 3 newest rows (unknown 3-bar look-ahead target), so the live RF/LSTM prediction was 3 bars stale. Added src/features.build_inference_features (build_features(drop_future_rows=False): drops NaN only on feature columns); training callers unchanged. signal_engine.predict_signal uses it and returns signal_time; LSTMTrader.predict uses it. app._build_signal_payload_uncached drops the forming 1h bar (paper_trader.drop_forming_bars) and adds signal_bar_time / signal_bar_age_minutes to each /api/signals row. tests/test_signal_freshness.py (3: timestamp within one bar of now, training still drops target rows, app wiring) added to TEST_FILES; parity + related 21 pass; System Doctor deep 180 passed. Backups *.bak_20260915-195342_execsafety4.
- 2026-09-15: Execution safety item 5 (autonomy confidence): src/execution_guard.is_real_broker_fill (outcome_source broker_fill + executed MT4/MT5 order + ticket, never demo/simulation modes). _auto_trade_execute_core tags trade_record.outcome_source (simulated_demo / broker_fill / broker_rejected). _jarvis_memory_record_execution_result sends simulated outcomes to simulated_exec_* only; real fills go to auto_exec_* with auto_exec_real_fills_only=True (older unmarked counters moved to legacy_auto_exec_*, nothing deleted); _jarvis_symbol_memory_adjustment ignores unmarked stats. Live state had no stats, nothing migrated. Gap (not invented): real fills are recorded as 'open' and no closed-trade result is fed back yet, so the memory shift stays 0 until that exists. tests/test_autonomy_confidence.py (4) in TEST_FILES; System Doctor deep 184 passed. Backup app.py.bak_20260915-201133_execsafety5.
- 2026-09-15: Execution safety item 6 (walk-forward on the shared engine): run_walkforward_backtest gains signal_mode ("live_engine" default = signal_engine.predict_signal per closed test bar with the fold's RF, live config; "rf_proba" = previous path kept); _simulate_trades takes the engine's directions; result carries signal_mode + note. tests/test_walkforward_live_engine.py (2: spy proves every test bar goes through predict_signal with the live config and every trade's side matches it; legacy flag + unknown mode) in TEST_FILES; walk-forward/parity/freshness 16 pass; System Doctor deep 186 passed. Backtests XAUUSD + BTCUSD 2y 1h recorded in BASELINE.md: both FAIL (PF 0.77 / 0.62, -58.6 % / -54.1 %), identical in both modes, long-only in practice. Backup src/walkforward_backtest.py.bak_20260915-203154_execsafety6.
- 2026-09-15: Execution safety final step: /api/signals returns stored history rows (a continuing HOLD is not re-recorded), so it cannot prove freshness. Added read-only GET /api/signals/live (live payload rows with signal_bar_time, signal_bar_age_minutes, fresh = age <= 120 min, all_fresh, checked_at); /api/signals response unchanged. tests/test_signals_live_api.py (2) in TEST_FILES; 13 related tests pass. Backup app.py.bak_20260915-214614_signalslive. App restarted twice via the start_trading.bat loop (stop only python app.py): 21:43:54 for items 1-6, then again for this endpoint. CLAUDE.md signal-flow section updated (items 4/6 fixed, execution guard described). auto_execute and autonomy stayed False throughout.
- 2026-09-16: The live system repo is https://github.com/th3emis31/ml-trading-system (private, default branch main). After every commit on a claude/* branch, run git push. Never push to main directly; merge through a pull request.
- 2026-09-16: /verify PASS before committing the claude/make-ml-trading-system-live branch: compileall clean (excluding the git-ignored _backups/ snapshot, whose old f-string and stale MT4Service copy cause 8 collection errors unrelated to this work), pytest -q --ignore=_backups 207 passed in 4:45, tests/test_system_doctor.py 9 passed, no risky deletions in the diff, the only "password" match is prose in a switch-script comment, System Doctor quick healthy (app PID 26348 answering on port 5000). The two scripts/claude-hooks/.dup-baseline-*.txt files stay uncommitted: they are machine-specific hook caches.
- 2026-09-16: Execution safety item 1b (approval bypass on /api/auto-trade/execute): the gate read "require_user_approval AND symbol in allowed_symbols AND not internal_auto_execute", so any symbol outside allowed_symbols (default XAUUSD/BTCUSD) skipped approval completely and went straight to the broker. allowed_symbols is a whitelist of what may be traded (src/tradingview_intake reads it that way at app.py:18470), so _auto_trade_execute_core now refuses a non-whitelisted symbol with 403 "not in the approved symbol list" (internal autonomy callers included) and approval_required is simply require_user_approval and not internal_auto_execute. auto_trade_recommend_api keeps its behaviour with the whitelist named explicitly. tests/test_approval_bypass.py (3, own _active_session_client helper because test_execute_api_security's client fixture is an inactive session) added to TEST_FILES; 18 related tests pass. Backup app.py.bak_20260916-163200_approvalbypass.
- 2026-09-16: Claude Code autostart (safe-upgrade): scripts\start_claude.cmd waits up to 60 s for the network (1.1.1.1) and the trading app (/api/self-test), then opens Windows Terminal in the live folder with tabs "bridge" (claude remote-control --spawn=same-dir) and "desk" (scripts\claude_desk.ps1: claude --continue, plain claude if nothing to continue), or two PowerShell windows when wt.exe is missing; log logs\start_claude.log. wt splits its command line on ";" even inside quotes, which cut the inline fallback off in the first test run, hence the .ps1 (a parallel edit that escaped ";" as "\;" and added --suppressApplicationTitle was kept for the titles). Task "SmartEntry Claude Code" registered through system_doctor.task_register_command: AtLogOn for DESKTOP-SBGFGM7\th_em, interactive token, Limited, no password; added to system_doctor.TASKS (new "onlogon" trigger kind, test asserts it) so --fix recreates it. Other 10 SmartEntry tasks and the Startup shortcut (start_trading.bat) verified unchanged. Test run 18:12:55: both tabs up (bridge PID 38752 with remote session, desk PID 34148 resumed this conversation) and /brain recall worked in the desk tab.
- 2026-09-16: INCIDENT - tests trained into the live models/. From 2026-09-15 18:38:59 local (first off-schedule learning run, absent from data/learning/daily_learning.log; git has no model history because models/ is ignored) tests/test_lstm.py (DailyLearner("XAUUSD"), LSTMTrader("BTCUSD").train), tests/test_pipeline.py (train_model XAUUSD) and tests/test_ensemble_integration.py (DailyLearner) wrote the live champions, archives, learning_decisions.json and <symbol>_daily_history.json because src/train.py, src/lstm_model.py, src/model_promotion.py and src/daily_learning.py used paths relative to the working folder. By 16 Sep 18:26 the live metrics read BTCUSD 0.840 / XAUUSD 0.611 (05:30 champion 0.556 / 0.532) and /api/signals ran on test artefacts (nothing traded: autonomy, auto-execute, session off). Fix: (1) polluted live files backed up to models/archive/<symbol>/20260916-173442-pre-restore-test-polluted; restored the last champions from the Daily Learning job run on real Yahoo candles before the pollution date = the 14 Sep 12:04-12:05 run (no 05:30-triggered run existed before 15 Sep; today's 04:30 archive was itself polluted: XAUUSD RF dated 15 Sep 20:09:15, BTCUSD LSTM 15 Sep 20:10), taken from archives xauusd/20260915-173859 and btcusd/20260915-173941 whose file dates prove them untouched (RF 14 Sep 12:04:44 / 12:05:17, LSTM 12 Sep 19:01), 14 files byte-identical, RF accuracy 0.5306 / 0.5565; restore logged with SHA-256 in data/model_restores.json. (2) src/runtime_paths.py (SMARTENTRY_MODELS_DIR / SMARTENTRY_DATA_DIR, defaults models / data, live app unchanged) used by those four modules; tests/conftest.py sets both to a temp folder before any src import, copies the live models there, and fails the session (pytest_sessionfinish) if any live model, learning_decisions, model_restores or *_daily_history file changes. (3) 28 of 31 learning_decisions rows (every run since 15 Sep 18:38:59, incl. the real 16 Sep 05:30 run that judged polluted champions) flagged test_polluted with a note, none deleted (backup learning_decisions.json.bak_*_prepollutionflag); src/learning_curve.py leaves them and their history rows out and reports excluded_test_polluted_runs; app.get_model_status skips them so "last trained" shows the loaded champion (backup app.py.bak_20260916-184207_modelstatus). (4) System Doctor check_model_integrity (every pass): fails when a live xauusd_*/btcusd_* file changed outside 05:25-06:30 local and its hash is not in data/model_restores.json; passes on the live folder now (14 files). Tests test_model_integrity.py (4), test_learning_pollution.py (3) added to TEST_FILES; targeted 31 passed. App restarted safely (PID 14580 -> 15984, /api/model-status accuracy 0.5306 / 0.5565). The 06:30 deep doctor never ran the offending tests (not in TEST_FILES): the pollution came from full pytest runs.
- 2026-09-16: CORRECTION to the "INCIDENT - tests trained into the live models/" line above: its cause is only partly proven. Tests (test_lstm, test_pipeline, test_ensemble_integration) could and did write live paths before isolation, but some off-schedule runs may have been on-demand retrains in the live app or another session; callers were not recorded, so the 28 decision notes now say "tests or an on-demand retrain, not proven" (backup learning_decisions.json.bak_*_attributionfix). The test isolation worked in the 18:41 suite (its runs used absolute temp paths); two further live runs at 18:43:06 (XAUUSD RF promoted) and 18:44:17 (BTCUSD LSTM promoted) had relative archive paths, i.e. the live app process or a remote Claude session, not a test. Investigated and ruled out as automatic triggers: auto-heal watchdog (self-test only), JARVIS autonomy loop (disabled), market scheduler (2 h analysis), performance cache worker, jarvis modules, page scripts (retrain only on click; JARVIS "Learning Status" / voice "check learning" run a full retrain); request log only on the start_trading.bat console, so the caller is not recoverable. Two remote Claude sessions were attached to this folder; the owner stopped the unknown one (bridge 41616/18752, worker 28560, started 18:27). Fixes: (1) src/runtime_paths.py LEARNING_WINDOW 05:25-06:30 (shared with System Doctor) and live_training_allowed(): DailyLearner.run_cycle now refuses BEFORE fetching or writing anything when the models folder is live, the time is outside the window and SMARTENTRY_ALLOW_OFFSCHEDULE_TRAINING is not 1, recording status blocked_outside_learning_window (not evaluate-only: training writes the challenger straight over the live files); (2) model_promotion.record_decision adds caller (pid, parent pid, argv, and HTTP method/path/remote_addr/user_agent/referrer inside the app) to every decision; (3) /api/train-daily and /api/train-weekly are POST-only with X-Control-Secret (GET 405, no secret 403); the learn page, training pipeline page and JARVIS runLearningUpdate now POST with the secret; (4) app DATA_DIR honours SMARTENTRY_DATA_DIR and tests/conftest.py copies data/ (without voice audio, logs, backups) to the temp folder and also fingerprints settings.json, knowledge.json, signals.json; (5) flagged the 18:43/18:44 runs (30 of 33 rows flagged), backed up the replaced files to models/archive/<symbol>/20260916-191615-pre-restore-offschedule-1843, restored the 14 Sep champions again (14 files byte-identical, restore log entry 2). tests/test_training_gate.py (5) added to TEST_FILES; test_app train test updated (GET 405, 403, POST+secret 200); targeted 49 passed. App restarted safely (15984 -> 34044): model-status accuracy 0.5306 / 0.5565, last_trained_at 2026-09-14 12:04:39 / 12:05:13, OPTIONS on both training routes = POST, OPTIONS, Model integrity OK. Backup app.py.bak_20260916-190903_traingate.
- 2026-09-16: /verify PASS before committing the live-folder sync + model-integrity work on claude/make-ml-trading-system-live: compileall clean (excluding git-ignored _backups/ and .claude/backups/), pytest -q --ignore=_backups 281 passed in 16:18 with the live app running (PID 34044), and against the 19:18:39 snapshot no live model/learning file changed, no new archive folder, decisions 33/33, System Doctor Model integrity OK; 12 flagged "deleted" definitions are all rewrites (DATA_DIR/MODELS_DIR/DECISIONS_PATH env-aware with the same defaults, SIGNAL thresholds from ENGINE_SIGNAL_CONFIGS, build_features/_symbol_curve/ensemble_predict signatures, TIMEFRAME_MINUTES gained 5m, two JS fetches now POST+secret, test_train_daily_endpoint stronger); secret scan 0 matches. Not committed on purpose: app.py.bak_* backups and the two scripts/claude-hooks/.dup-baseline-*.txt machine caches. tests/conftest.py no longer fingerprints data/signals.json (the running app legitimately appends to it).
- 2026-09-16: Locked the remaining retrain paths (owner request): POST /api/chart-learn/<symbol> (was GET; now 405 on GET, 403 without X-Control-Secret), /api/quality-retrain and /api/screenshot-learn (when retrain is requested; retrain=false still stores the screenshot and notes) need the control secret via _training_request_refused; the JARVIS intents check_learning and system_update (system_update also retrained, found while locking) only run DailyLearner.run_cycle when _learning_retrain_secret_ok(), otherwise they answer read-only from get_model_status. Page callers send the secret: dashboard quality-retrain buttons and JARVIS executeCommand read window.SMARTENTRY_CONTROL_SECRET or localStorage smartentry_control_secret (not injected on those pages, so without it they get 403 / read-only), the learn page screenshot upload uses the injected control_secret. tests/test_retrain_routes_locked.py (4, a stand-in learner fails if anything trains without the secret) in TEST_FILES; targeted 28 passed; full suite 285 passed with the app running, live models/archives/decisions unchanged against the 19:53 snapshot. /verify PASS (one rewritten fetch line, 0 secret matches). Backup app.py.bak_20260916-195050_retrainlock. The live app still runs the previous code until the next safe restart.
- 2026-09-16: Event defence (owner /safe-upgrade after the 18:00 UTC FOMC candle; dry run, no order path changed): src/event_defence.py adds the tier-1 list (FOMC decision, press conference, minutes, CPI, NFP) with no new entries 60 min before to 90 min after and a 60-min pre-event "reduce or flatten" alert for XAUUSD/BTCUSD, plus a volatility breaker (H1 range > 3x ATR14 of the previous bars blocks entries on the next 3 bars). GET /api/event-defence reports both per symbol from the live calendar and broker H1 bars and logs each breaker/window/alert once to data/event_defence_log.jsonl; it is not wired into /api/auto-trade/execute. data/historical_events.csv (413 rows, 2018 to 16 Sep 2026, Fed FOMC calendars and BLS CPI/Employment Situation schedules, 08:30/14:00 ET converted with US DST; 3 unscheduled 2020/2025 FOMC actions kept without a time and skipped; October 2025 CPI/NFP never published) is tracked in git via a .gitignore exception. src/gold_session_pullback_lab.py backtests the strategy doc with the tier-1 window applied on history (variants: none, tier-1, tier-1 + breaker; inverse baseline; locked XAUUSD 1h holdout). Measured candle: range 110.13, 7.17x ATR14 (LESSONS.md). Tests: test_event_defence.py (8), test_gold_session_pullback_lab.py (6), both in System Doctor.
- 2026-09-16: Learning gate clock + task marker (owner request before the event-defence commit). Measured first: the old gate compared datetime.now() (Windows local, BST) with 05:25-06:30, so the 17 Sep 05:30 local run was inside it, not refused as feared; the change was still made. src/runtime_paths.py now builds the window from the task trigger DAILY_LEARNING_TRIGGER_LOCAL "05:30" (-5/+60 min) on the local date, converts it to UTC at runtime with that date's DST rules, and compares now in UTC (17 Sep: 04:25-05:30 UTC). Inside the window a cycle also needs a fresh marker data/learning/daily_learning_task_marker.json (task "SmartEntry Daily Learning", started_utc inside this window, <= 60 min old), written by scripts/run_daily_learning.cmd via `python -m src.runtime_paths --mark-task "SmartEntry Daily Learning"` before training; on-demand retrains in the window are refused unless SMARTENTRY_ALLOW_OFFSCHEDULE_TRAINING=1. System Doctor model integrity uses the same UTC window. Tests in test_training_gate.py: tomorrow's real 05:30 local run passes with its marker, missing/stale/foreign/early markers refused, BST/GMT/change-day windows, script order and trigger match. Marker CLI dry-run into a temp data dir OK; no live marker written.
- 2026-09-16: /verify PASS before committing event defence + learning gate clock/marker: compileall clean (excluding backup folders), pytest -q --ignore=_backups 303 passed in 15:01 with the live app running, live models/ + data/learning + learning_decisions.json (394 files) unchanged before/after, no secrets in diff, removed definitions only rewritten (LEARNING_WINDOW, live_training_allowed, one window test).
- 2026-09-16: Gold session pullback backtest run (commit 7d6d3c6, broker H1 2018-04 -> 2026-09-16 18:00, locked holdout from 2025-01-08): all three variants FAIL the success bar - holdout exp +0.028R / +0.032R (tier-1) / +0.039R (tier-1 + breaker) on 79/75/73 trades, deflated Sharpe 0.60-0.65, development 2018-2024 negative (-0.03R, 2019 and 2021 about -9R each); 80 % of trades end at the 21:00 time stop. Rows in BASELINE.md; result data/strategy_lab/gold_session_pullback.json. Also at owner request the gold 4h model demo executor (magic 440401) was switched off in data/paper_trading/demo_execution.json (enabled false + disabled_reason, backup .bak_*, journal event 'disabled' 19:50:53 UTC, no open position); its code is kept and every skipped signal now logs the reason.
- 2026-09-16: Demo trading for the gold session pullback (owner /safe-upgrade; decisions asked: two 0.01 legs because 0.01 cannot be split, kill switches on this strategy's own P&L by magic, build with sending OFF until the backtest is shown). src/demo_session_pullback.py reuses the lab's setup/exit functions; hard-coded DEMO_ACCOUNT_LOGIN 11581419 (login + trade_mode 0 + demo server, re-checked on every order/close/stop change by DemoOnlyEngine), MAGIC 440502 (unused among the account's 32 magics), 2 x 0.01 lot legs TP 1.5R / 3R, break-even for leg B after leg A's target, one trade, 2/day, flat 21:00 UTC; gates: session, tier-1 window (historical csv + live calendar), breaker, 30-min High-impact window, spread <= 25 % of R, R 0.5-4 ATR; kill switches -3 % day stop / 15 % drawdown halt on the strategy's P&L; any MT5 error (disconnect, unreadable account/positions/deals, non-MT5 bars, rejected order/close/modify) halts until owner resume; a rejected second leg closes the first. No approval queue; every decision to data/paper_trading/demo_session_pullback_log.jsonl; closed trades to data/trade_memory/gold_session_pullback_demo.jsonl (magic, setup, session, regime, next tier-1 event distance, R) - kept apart from the JARVIS autonomy confidence memory so demo results cannot move auto-execution thresholds. MT5Service gained account_snapshot() and modify_position_sltp(). App: POST /api/demo-trading/cycle|stop|resume (local + control secret), GET /api/demo-trading/status, page /demo-trading (Automation nav) with STOP (halt + close magic 440502 legs only) and two-click Resume. Task SmartEntry Demo Pullback hourly :01 (scripts/run_demo_pullback.cmd), System Doctor check_demo_pullback + TASKS entry. Config data/paper_trading/demo_session_pullback.json: enabled true, dry_run TRUE. Tests tests/test_demo_session_pullback.py (14).
- 2026-09-16: /verify PASS before committing demo trading: compileall clean, pytest 317 passed in 10:58 with the live app running, live models/learning files (394) unchanged, no secrets, only removal = demo_executor.load_config gained a defaults parameter.
- 2026-09-17: Daily plan check (owner: 'DAILY PLAN TV FOR TRADINGVIEW CHECK IS WORKING'). /api/tradingview/plan XAUUSD+BTCUSD available from mt5 bars, fresh (10:51 UTC, last closed H4 05:00), status no_trade with checklist identical to the TradingView SmartEntry Daily Plan table (x trend x push x pullback v close v RSI). BUG found and fixed: src/daily_report.analyse_levels took d.iloc[-2] as 'previous day' and excluded the last row from the 5/20/55-day windows, but both callers (daily report, tradingview plan) pass CLOSED days only, so every level was one day stale (17 Sep plan showed 15 Sep H/L/C 4317.38/4261.29/4293.45 instead of 16 Sep 4367.47/4235.17/4264.08; TradingView table had 4367.415/4235.165/4263.855). Also nearest support/resistance now compares with the rounded close so a level at the close is not picked. Test test_previous_day_levels_use_the_last_closed_day; test_daily_report + test_tradingview_plan + test_obsidian_notes 15 passed. Remaining known differences (not bugs): TradingView table's nearest S/R and daily RSI come from the Pine indicator's own calculation on OANDA data incl. the forming day (RSI 42.3 vs system 28.4 on closed days); plans are only saved when the API is called (no file for 16 Sep).
- 2026-09-17: Volatility Trend Breakout into the system (owner: "use it for the system"; decisions asked: send demo orders now, run beside the gold session pullback, fixed 0.01-lot legs). The owner's Pine v5 "Best Version" was replicated first on broker 4H (2023-2026 PF 2.023 vs TradingView 2.027; 2007-2022 PF 0.97 - porting doc gate "regime check outside 2023-2026" NOT passed, owner chose demo anyway; demo only, no real money). src/volatility_trend_breakout.py = deeptrade-ai c9fffc2 port copied unchanged (CLI left out, parity rerun identical). src/demo_volatility_breakout.py: hourly :03 cycle through the app (POST /api/demo-breakout/cycle|stop|resume + status, local + control secret), 4H closed candles from MT5 only, signal on the last closed candle within 45 min, price drift <= 0.5 ATR, spread <= 10 % of R, ask < TP1, tier-1 window, H1 3xATR breaker, 30-min news window; 2 x 0.01 lot BUY legs, magic 440603: A TP1 1.3R, B TP2 2.8R, both SL signal close - 1.5 ATR on the broker at entry (the script has no stop on the first bar); after A fills, B stop -> signal close + 0.15 ATR then trail 2.2 ATR below the high since entry (current ATR, as the script); 65 closed 4H candles -> close; one position; kill switches on its own P&L (-3 % day, 15 % DD halt) and any MT5 error halts; trade memory data/trade_memory/volatility_breakout_demo.jsonl. Reuse: demo_session_pullback log/_halt/_mt5_error/update_kill_switches gained sink=, DemoOnlyEngine gained magic/volume (defaults = pullback). /demo-trading page has a breakout section with its own STOP/Resume. Task SmartEntry Demo Breakout + System Doctor check (check_demo_pullback parameterised). Config data/paper_trading/demo_volatility_breakout.json enabled true, dry_run FALSE. Tests tests/test_demo_volatility_breakout.py (7), fixture fresh_files shared.
- 2026-09-17: /verify PASS before committing the volatility breakout demo integration: compileall clean, pytest in four foreground parts (a background run was stopped by the task memory monitor at ~1 GB free) 127 + 109 + 76 + 15 = 327 passed, live models/ + data/learning + learning_decisions.json (409 files) unchanged, no secrets, flagged removals are only signature extensions (log/_halt/_mt5_error/update_kill_switches sink=, DemoOnlyEngine magic/volume).
- 2026-09-17: TradingView chart analysis (owner: market analysis for both assets with entry/SL/TP and previous days' levels, 'i want trading view chart'). New indicator strategies/tradingview/smartentry_market_map.pine ('SmartEntry Market Map', saved in the owner's TradingView account v1 16:27 local, compiled without errors, added to layout SmartEntry XAUUSD beside the existing scripts; old SmartEntry Daily Plan script kept). Closed candles only (forming candle -> previous closed values). Draws: Volatility Trend Breakout plan (4H close trigger = 20-bar high + 0.35 ATR, stop 1.5 ATR, TP1 1.3R, TP2 2.8R, trail, open-trade tracking), Swing Trend Pullback zone/stop/target, prev day H/L/C, prev week H/L, 5/20-day H/L, daily pivots, swing S/R. Table XAUUSD + BTCUSD side by side (daily trend EMA50/200, 4H vs EMA50, RSI D/4H, daily ATR %, prev day H/L, breakout trigger + ATR distance, status, volume) plus this chart's plans. First read 15:28 UTC: gold ARMED above 4396.86 (0.5 ATR), BTC WAIT below EMA50; prev day high 4367.42 matches broker 4367.47. Premarket gappers scanner premarket_gappers.sh also built (first run failed on a --disallowedTools argument bug, fixed, not rerun per owner instruction).
- 2026-09-17: Finish (owner: 'finish'): (1) premarket_gappers.sh run once after the fix: Yahoo step OK (10 names: GNRC +19.15 %, SDGR +17.6 %, VICR +15.38 %, ...) but every Benzinga catalyst null - a direct test fetch returned HTTP 403 Forbidden, so the owner's named catalyst source is blocked; output premarket_gappers_2026-09-17.json. (2) TradingView layout SmartEntry XAUUSD: removed 6 stale drawings by id via the chart API (INVALIDATION 4365.57, SL 4367.5, TP1 4241.45, TP2 4183.85, TP3 4099.12, text note '15 Sep 2026 06:16 UTC | SwingTrendPullback H4 ...'); kept 3 August rectangles, the 15 Sep trendline, the two already-hidden ENTRY lines, paper positions and the Market Map; layout saved. (3) Plan journal src/plan_journal.py + tests (4) + GET /api/plan-journal + task SmartEntry Plan Journal hourly :07 (scripts/run_plan_journal.cmd) + doctor TASKS; results in BASELINE.md. Suite 331 passed (fast 316 + walk-forward 15, foreground), live files unchanged.
- 2026-09-17: Atomic Analyst V85 check + fixes (owner asked; the panel is an MT5 indicator on chart XAUUSD H1, account 25446287, not the demo the system trades). Audit: compiles 0/0, live (JSON rewritten each tick), vote maths verified against the screenshot (5 buy indicators + 3 buy TFs + atomic trend = 9/17 = 53 %; 1 sell = 6 %; 41 % wait), stats verified (33 closed, 14 wins = 42.4 %, PF 1.371, +206.72 = 20,672 points x 0.01), last ticket matches AtomicAnalyst_Signals.csv, handles validated at init (a failed handle falls back to WAIT), feedsTheGate false. Fixed in ATOMIC_ANALYST_V85.mq5 (backup .bak.20260917_*, recompiled 0 errors/0 warnings): the decision note was cut off ('confirmed by') - now two lines; headline 'BUY ONLY' now reads ALIGNED only with a 6-of-9 majority, otherwise '(no TF against)' or '(MTF MIXED)', and the MTF Alignment row shows the buy/sell count; statistics labelled 'Signal result (chart sim)' / 'Profit factor (chart sim)' with 'price' units. NOT fixed (needs the owner): push alerts fail with err 4517 - set the MetaQuotes ID in MT5 Tools > Options > Notifications; and MT5 did not auto-reload the indicator after the recompile (charts still ran the old build), so the chart needs a refresh. System sync: src/atomic_analyst.py reads the panel's JSON (staleness 30 min) and compares its verdict with the plan journal's armed breakout, GET /api/atomic-analyst, System Doctor check_atomic_analyst; read-only, feeds_orders false, tests tests/test_atomic_analyst.py (3). Suite 319 + 15 passed, live files unchanged.
- 2026-09-17: Positioning page (owner asked for a higher-quality version of the Greek TikTok dashboard; chose 'positioning page for gold/BTC'). src/positioning.py downloads and caches the CFTC Legacy futures-only COT files (6 years), computes net, % of open interest, week change, 6-year percentile and crowding per market for GOLD, SILVER, BITCOIN, ETHER, EUR, GBP, JPY, AUD, CAD, CHF plus a derived USD proxy (no ICE dollar index in that report). GET /api/positioning + page /positioning (nav Trading), task SmartEntry Positioning daily 21:10 (scripts/run_positioning.cmd, release is Friday 15:30 ET), System Doctor check_positioning warns above 14 days. runtime_paths.read_latest_json now shared with plan_journal. Values match the owner's screenshot for the same week. Tests tests/test_positioning.py (5). Suite 324 + 15 passed, live files unchanged.
- 2026-09-17 ~21:05: Positioning page raised to a professional standard (owner: "higher quality deep search ... proffesional improvment"). src/positioning.py: cache schema 2 (commercials 11/12 and non-reportable small traders 15/16 now stored, version-1 caches refetched automatically), Williams cot_index() over 156 weeks, _change() for 4- and 13-week net changes, open_interest_change, flipped, 104-week sparkline, and forward_study(): for every past week the COT index is recomputed from data available then and the next 4 weeks of broker candles measured, bucketed into crowded short (<=20) / middle / crowded long (>=80) with an evidence label under 30 weeks. Page rewritten: focus cards for XAUUSD and BTCUSD (net, COT index and percentile bars, sparkline, 1w/4w changes, commercial side, "what happened next" table), full market table with 1w/4w/13w, commercials, index, percentile and sparkline, dollar proxy, and a glossary explaining every column. Measured on gold, 2020-2026: after a crowded-long reading (index >= 80, 37 weeks) gold averaged +2.43 % over the next four weeks and rose 75.7 % of the time - the opposite of the usual "crowded = reversal" story, so the page states it descriptively and claims nothing. Tests tests/test_positioning.py 4 -> 7 (schema refetch, cot_index/_change limits, forward_study bands and thin-evidence label, page glossary labels). Suite 341 passed (332 fast + 9 walk-forward), 409 live model/learning files unchanged.
- 2026-09-17 ~21:35: /demo-trading raised to the same standard (page 2 of the one-by-one pass). Two gaps closed: the page never showed the demo account itself, and a strategy whose scheduled task had stopped looked identical to one finding no setup. src/demo_executor.py gained account_view() (balance, equity, open profit of that strategy's legs, currency, demo flag, or available:false with the reason - never a remembered number) and cycle_health() (age of the last cycle against the 60-minute task interval, "late" past twice that). Both status payloads now carry "account" and "cycle_health"; the breakout payload reads the account snapshot too. Page: a "Demo account" card above both strategies (balance, equity, open profit summed over both magic numbers), the last-cycle tile shows the age and says "late" when the task stops, and the decision log collapses consecutive runs of the same decision (20 no_setup cycles became one row, with "show every cycle" keeping all 24). Verified in Edge: balance 88,582.15 GBP, equity 88,568.48, cycle "running on schedule, every 60 min". Tests +2 in tests/test_demo_executor.py; 67 passed across app/demo/positioning.
- 2026-09-17 ~22:05: /signals raised (page 3 of the one-by-one pass). The card showed "Confidence 55 %" for a HOLD, which said nothing about why the model stood aside, and the recorded signal history was not on the page at all. app.py: _model_evidence() passes the engine's own numbers into /api/live-plan/<symbol> (blended probability, RF and LSTM probabilities, the live config's BUY 0.55 / SELL 0.45 thresholds, distance to the nearer line, the signal bar time and age, model accuracies) - read from src.signal_engine.get_config('live'), so the page can never drift from the engine. Card: a probability scale showing where the blend sits between the SELL and BUY lines (XAUUSD 0.5239, 0.0261 from the nearer line), and a bar-age chip that turns red past 120 minutes. New card "Recent signals and how they settled": every recorded signal with its blend/RF/LSTM, the outcome from evaluate_signal_outcome, and a tally that currently reads 0 win / 4 loss of 4 settled, with a note that under 100 settled signals this is a record and not evidence. Tests +1 (tests/test_signals_live_api.py); 28 passed across app/parity/freshness/live-api.
- 2026-09-17 ~22:20: i40 Pilot built - the owner's name for the system brain ("Jarvis the need to know everything about the system is the big brain, with memory, brain, context, loop, skills, tools, Instruction, schedule, Signature Rules"). src/i40_pilot.py assembles one brief from the system's own sources: identity (folder, branch, commit), 10 signature rules written in code with the reason and the date each was set, instructions, memory (BASELINE row count + latest rows, NOTES tail, LESSONS, open BACKLOG), context (both demo strategies through the app's HTTP API - never a second MT5 connection - plus models, learning decisions per symbol, positioning), schedule (all 14 tasks with last/next run from schtasks), skills (.claude/skills), tools (11 CLI entry points + 250 API endpoints from app.url_map) and loop (age of the brief, stale past 2 h). Read-only by construction: no order, no training, one file written. Page /i40-pilot + GET /api/i40-pilot (?live=1 rebuilds), nav entry, task "SmartEntry i40 Pilot" hourly :40 (scripts/run_i40_pilot.cmd), doctor check check_i40_pilot. Tests tests/test_i40_pilot.py (7). First run: 88 recorded results, 12 skills, 14 tasks none missing, pullback dry run, breakout sending orders.
- 2026-09-17 ~22:10: premarket_gappers.sh catalysts fixed. Benzinga returns HTTP 403 to every automated request (re-checked today), so the catalyst fetch now tries stockanalysis.com first and Benzinga second, and each row records which source answered in catalyst_source. Gainers parsing was never the problem (10 names both runs).
- 2026-09-17 ~23:45: /i40-pilot redesigned after the owner said the first version "is not really good at all" and told me to check online. Researched how agent-observability and operations consoles are built (MLflow LLM observability guide, Brent Haskins on agent observability as a product-UX problem, operational dashboard layout guides) and took three rules from it: status before detail, error classification instead of error counts, and a state + stuck-items + activity-trail trio for situational awareness. The page is now a control room: one colour-coded verdict line, six health tiles (strategies sending/dry, demo equity, tasks registered, learning, recorded results, brief age), "Needs attention" naming each problem with why it matters and where to look, a strategies panel with mode/cycle/last decision, and an activity feed merging the strategies' decision logs, the scheduled tasks' last runs and the learning decisions, newest first. Rules, schedule, memory, skills, tools and context detail moved into collapsed sections (progressive disclosure) instead of a wall of text. src/i40_pilot.py gained attention() (worst-first, empty when nothing is stuck), activity() and _as_iso() (Task Scheduler writes dd/MM/yyyy and 30/11/1999 for "never ran" - both handled). Mobile: checked at 500 px, the layout already fits; the earlier "cut off" phone screenshot was headless Edge's minimum window width, not a CSS fault. Tests 7 -> 9.
- 2026-09-18 ~08:15: Health check run twice. Deep check last night: healthy, 18 OK / 2 info / 0 warn / 0 fail, 54 files compile, 273 tests pass. This morning one warning: "only 441 MB RAM free ... watch for a memory leak" against the app process. Measured it instead of repeating it: msedge 1,763 MB across windows, claude 721 MB, all python 646 MB of which the app itself 509 MB (started 23:34, 8 h earlier) - the pressure was the browser and the editor sessions, not the trading app. So src/system_doctor.py now attributes memory before implying a leak: process_memory() reads tasklist (no psutil on this machine) and sums the working set per process name, check_resources() names the three largest users in any low-RAM warning, and apply_ram_trend() reports the app process's own working set (ram_trend.app_rss_mb) beside the drop. The old "watch for a memory leak" wording is gone because it guessed. tests/test_system_doctor.py 9 -> 10 with a canned tasklist.
- 2026-09-18 ~08:55: Gold Reaper forward test set up after the owner's tester report (ReportTester-25446287.xlsx) and the decision to run it on demo 11581419. First, cost stress on the report's own 279 deals: total traded volume is only 3.89 lots over 5.77 years, so commission at $7/lot costs $27 against $1,239 profit and PF stays 2.030; even $2/trade slippage leaves +$654 at PF 1.462. Costs are not what threatens this EA - in-sample fitting is. New read-only module src/gold_reaper_watch.py: reads closed deals through the app (new GET /api/mt5/deals?days&magic&comment_contains, so no second MT5 connection), keeps deals whose magic is 8000 or whose comment contains "Gold Reaper" on XAUUSD, journals each ticket once into data/gold_reaper_watch/trades.jsonl, and reports the forward record (net after swap+commission, win rate with a Wilson 95 % interval, PF, average trade, worst run-down) beside the vendor backtest with its caveats. Evidence bar: under 30 trades nothing is claimed, 30 = interim, 100 = verdict. Page /gold-reaper + nav entry, task "SmartEntry Gold Reaper Watch" hourly :45, doctor TASKS + TEST_FILES. Tests tests/test_gold_reaper_watch.py (4). First run: 895 deals scanned on the demo account, 0 by this expert - it is not attached there yet, which the page says plainly. Note: the demo account already carries other EAs' magics (10002, 888888, 10003, 202503, ...) and showed +335.54 open profit from positions that are not this system's.
- 2026-09-18 ~09:10: Gold Reaper watch removed the same morning it was built. The owner: "don't touch anouthes EA Can you just focus what the system have do not record another ea only what the system have". The standing rule was that other experts are never edited or restarted; it now also covers tracking - no journalling their deals, no filtering account history by their magic numbers, no pages or health checks built around them. Removed: src/gold_reaper_watch.py, tests/test_gold_reaper_watch.py, scripts/run_gold_reaper_watch.cmd, data/gold_reaper_watch, the /gold-reaper page and /api/gold-reaper, the nav entry, the doctor TASKS + TEST_FILES entries, and GET /api/mt5/deals (it existed only to read other experts' history; the system's own strategies call engine.deal_history directly and are unaffected). Scheduled task "SmartEntry Gold Reaper Watch" deleted - back to 14 tasks, doctor healthy 16 OK / 2 info / 0 warn. The BASELINE row assessing the owner's tester report stays: it is a dated record of a file they handed over, not machinery watching another EA.
- 2026-09-18 ~09:30: premarket_gappers.sh fixed properly - catalysts now come back 10/10. The real cause was not the news source: Windows Python prints CRLF, mapfile kept the carriage return in each symbol, so the script wrote news_GNRC<CR>.txt while the assembler looked for news_GNRC.txt and recorded "catalyst not found" for every ticker. Fixed with `| tr -d "\015"` on the symbol list. (Benzinga's HTTP 403 to automated requests is real and the earlier swap to stockanalysis.com stands, but it was not what produced the nulls - the CR was.) Also added GAPPERS_KEEP_WORK=1, which keeps the temporary folder with the raw fetch replies; without it a failed run left nothing to diagnose, which is why the first two attempts were guesswork. Verified: 10 gappers, all with a catalyst and headlines sourced from stockanalysis.com, e.g. GNRC +18.34 % on an $8 bn Amazon supply agreement, BHVN +15.56 % on an FDA partial clinical hold.
- 2026-09-18 ~09:45: Three checks for the owner. (1) Why the demo pullback finds no setup: measured, not guessed, over 5,984 broker H1 bars (2025-09-12 to now). The binding filter is the rejection candle, not the trend: 41.3 % of bars never touch the EMA20, 38.3 % fall outside 07:00-19:00 UTC, and of the bars that do touch in the trend direction only about one in five produces a qualifying rejection candle - 59 setups in 12 months, about 5 a month. Last 14 days: 129 session bars, 20 touched the EMA20, 19 failed the rejection test, 1 setup (2026-09-08). So "no setup" is the strategy behaving as designed and the demo executor has only been running since 16 Sep; nothing is broken. Loosening the rejection filter is a parameter change and would need a lab sweep on train data plus the 0.95 bar, not a live edit. (2) TradingView daily plan works for both symbols (mt5 bars, last closed 4H candle, checklist per condition), but nothing refreshed it on a schedule - the file was only rebuilt when someone opened the page, so the Obsidian vault could carry a stale plan. Added scripts/run_tradingview_plan.cmd + task "SmartEntry TradingView Plan" hourly :45 (rebuilds both symbols through the app) and registered it in the doctor. (3) SmartEntry Market Map is live on the chart and updating (table showed "Updated 09-18 08:44"); improvements added to strategies/tradingview/smartentry_market_map.pine: compact-table input, background transparency input, and a "time to the next 4H close" row so an ARMED plan says when it resolves. Pasted and saved in the TradingView editor (compiled clean, version 2), but the on-chart instance still runs the previous build - TradingView does not swap a running instance on save; it needs one "Add to chart" from the editor.
- 2026-09-18 ~10:00: /tradingview improved (page 5 of the one-by-one pass), and it carried a real honesty bug. Every alert that arrived without a BUY or SELL side was labelled "Divergent" - a verdict that the system's model pointed the other way - when in truth there was nothing to compare: 4 of the 6 stored alerts were sideless tv-plan payloads. build_tradingview_plan() now labels those "No side to compare" with the reason, keeps Aligned/Divergent for alerts that do carry a side, and "Unknown" when the model has no signal. The safety gate is unchanged: a sideless alert is still refused by the side check, not by alignment (asserted in the new test). The page's tile now reads "Aligned with model 0 of 2 · 4 carried no side" instead of "0 of 6", and the Last alert tile shows the age with a plain warning when the webhook has been quiet - it currently reads "3 days ago - the webhook is quiet". Tests tests/test_tradingview_intake.py 5 -> 6.
- 2026-09-18 ~10:15: The owner asked for a 24/7 market-watching agent for both assets with memory, brain, context, loop, skills, tools and instructions - and said to check before building. Checked: it already exists. src/daily_agent.py (511 lines) researches broker candles through the app, the daily plan with the owner's TradingView inputs, the economic calendar and the ATOMIC panel, decides BUY/HOLD/CLOSE/WAIT/NO_TRADE/SKIP_NEWS once per closed H4 candle for XAUUSD and BTCUSD, and journals every decision including the do-nothings; it has no live mode and imports no broker service. The real fault was where it ran: the "SmartEntry Daily Agent" task still pointed at C:\Users\th_em\scripts\run_daily_agent.cmd, the fallback copy, so since the 16 Sep folder switch it had been journalling into the fallback (44 entries to 2026-09-18 05:10) while the live folder's copy froze at 2026-09-16 13:10 and the live /daily-agent page showed nothing. Migrated the fallback history into the live folder (live copy backed up first as data/daily_agent.bak_20260918-100745; no trades were taken in either, balance 10,000 untouched) and added scripts/run_daily_agent.cmd for the live folder. Repointing the scheduled task itself was refused by the permission layer (Unauthorized Persistence), so the owner has to run the one schtasks /Change line. Root cause that let this hide for two days: the System Doctor only checked that tasks exist and last returned 0 - never that they run from the live folder. check_scheduled_tasks() now reads "Task To Run" and warns when any SmartEntry task runs from outside it; it currently flags SmartEntry Daily Agent and SmartEntry CRT Forward. Tests 10 -> 11.
- 2026-09-18 ~10:35: /daily-agent (page 6 of the pass). The migration worked - the page now shows all 44 decisions for both assets with the plan, checklist and reasons. Added the two clocks it was missing: the task runs hourly but a decision is only taken when an H4 candle closes, so a four-hour-old decision is normal while a four-hour-old run is not, and the page said only "Agent last run". It now reads "Agent last ran 08:10 UTC (73 min ago) · last decision 05:10 UTC (253 min ago)" and turns red when either is late. src/daily_agent.py gained run_health (60 min cadence) and decision_health (240 min). Two consolidations on the way: cycle_health now accepts minute-precision stamps as well as seconds, and it moved from src/demo_executor.py to src/runtime_paths.py - tests/test_daily_agent.py::test_agent_has_no_order_path forbids the agent importing demo_executor at all, because that module can place orders, and the test was right to stop me. demo_executor re-exports it, so nothing else changed. 60 tests pass across agent, executor, pullback and app.
- 2026-09-18 ~11:00: Fixed the two voices talking over each other. Cause found by reading the code, not guessing: GET /api/morning-briefing spoke the whole briefing through the PC's own Windows voice (jarvis_voice_response -> pyttsx3/SAPI, which is installed here) and also returned the text, and the browser then read the same words aloud 400 ms later in the voice the page had chosen (EA_PANEL_TEMPLATE runMorningBriefing). Two engines, two voices, same sentence. The endpoint now speaks only when a caller asks with ?speak=1 (for callers that have no browser) and reports spoken_by_server in its payload; the browser keeps speaking as before, alone. Also tidied the page speakers while looking: the JARVIS voice page's speakResponse - the one that owns the queue, the chosen voice and the startup lock - was never exported, so it now sets window.speakResponse (and window.stopJarvisSpeech), and the dashboard's simple jarvisSpeak routes through it when present instead of starting a second, unqueued utterance. Test tests/test_voice_single_speaker.py added and registered in the doctor's TEST_FILES.
- 2026-09-18 ~11:20: Correction to the voice fix. Silencing the server side was the wrong half: the PC voice (pyttsx3 / Microsoft Hazel) is the one that reliably plays, because a browser will not speak until the page has been interacted with, so the owner was left with no voice at all. Restored: /api/morning-briefing speaks through the PC by default again and reports spoken_by_server: true, and the page now skips its own speech when it sees that flag - one voice instead of two, and it is the one that works. ?speak=0 hands the reading back to the browser for anyone who prefers it. Engine verified in this process: 2 installed voices (Hazel GB, Zira US), rate 200, volume 1.0, queue drains after speaking. Lesson for next time: when two things overlap, check which one the owner can actually hear before muting either.
- 2026-09-18 ~11:35: /daily-report checked number by number against the broker candles (page 7 of the pass). Two things looked wrong; one was not. (a) London and New York showing identical high/low/range for BTCUSD is correct: the session windows overlap (Asia 00-08, London 07-16, New York 12-21 UTC) and the day's high fell at 12:00 with the low at 13:00, inside both. The page now says so instead of leaving it to look like a repeated row. (b) The report genuinely looked a day stale - "last daily bar 2026-09-16" on a report written 2026-09-18 - and the cause was labelling, not data: this broker's daily candle opens at 21:00 UTC, so the candle stamped 16 Sep 21:00 closes 17 Sep 21:00 and holds Wednesday's trading, which is the right bar to use (the 17 Sep 21:00 candle is still forming until tonight). Added _daily_bar_window() and the tile now reads "daily candle 2026-09-16 21:00 → 2026-09-17 21:00 UTC (the 2026-09-17 session)". Test added in tests/test_daily_report.py (8 -> 9).
- 2026-09-18 ~12:00: The two voices came back ("woman and men") because the briefing was only one of six speakers. Found by listing them all instead of patching another endpoint: server side, jarvis_voice_response speaks for the morning briefing, jarvis_market_scheduler's market announcements and jarvis_voice_learning_integration; browser side, the dashboard's jarvisSpeak, the JARVIS voice page's queued speakResponse and the EA panel's fallback speakResponse. The PC voice is female (Microsoft Hazel), the browser default male - hence a woman and a man reading the same sentence. Fixed with one setting instead of six patches: data/voice_config.json {"speaker": "pc"|"browser"}, default pc. jarvis_voice_response.speak() returns early unless the choice is "pc", which covers every server-side caller including the scheduler; a small script in the shared nav gives every page window.jarvisBrowserMaySpeak(), and all three browser speakers check it. GET/POST /api/voice/speaker reports and switches sides and refuses anything but pc or browser. Tests: tests/test_voice_single_speaker.py 1 -> 3, including that "both" is rejected, since that is the state the fix exists to prevent.
- 2026-09-18 ~13:45: New identity chosen and built: KEEL ("what keeps a boat upright when the wind picks up - it does not make it fast", which is what the gates, kill switches and the 0.95 bar are). Checked names online first: Sentry and Aegis are already trading/fintech brands, so both were dropped; Meridian, Candela, Keel and Lodestar are clear, i40 is the owner's own coinage. Built: static/keel_mark.svg (deck line, hull curve, keel), a new page /keel with the KEEL identity on the deep-fade background, and a nav entry. The page shows the state line, five plates (strategies, demo equity, scheduled work, recorded results, brief age), a voice bar that switches the single speaker and reads the briefing, needs-attention, strategies and the activity feed - all from /api/i40-pilot and /api/voice/speaker. Nothing was deleted: the original voice console stays at /jarvis-voice and is linked from the page footer.
- 2026-09-18 ~13:50: The new page immediately surfaced a real fault. Both demo strategies HALTED themselves today - gold_session_pullback at 12:01:11 and volatility_trend_breakout at 12:03:14 - with kind=account_refused: "logged-in account 25446287 is not the configured demo account 11581419". The MT5 terminal had been switched to account 25446287 (the account the Gold Reaper tester report came from), and the account guard did exactly what it was built to do: refused to trade and stopped. The terminal is back on 11581419 now (equity 88,802.44 GBP), but both strategies stay halted until the owner resumes them, which is their decision, not mine.
- 2026-09-18 ~13:00: KEEL page rebuilt after the owner said the first attempt was "no really good the need much better" - it was the same cards with a new name on them. Researched what separates a premium product surface (glassmorphism guides, 2026 UI trend write-ups) and applied it: frosted panels with backdrop blur and an inner top highlight over an aurora field of three radial gradients, one very large statement of state with a pulsing status dot, a live UTC clock with the Asia/London/New York strip lighting the sessions that are open (they overlap, so two can be lit), a metric rail with gradient edges, market cards with filled sparklines from /api/price-history, a voice bar, and glass panels for attention / strategies / activity. Three faults fixed in the second pass: the account block was reading the hour-old brief and showed the wrong account (94,402 GBP on 25446287) so it now reads /api/mt5/account live and turns red when the terminal is not on demo 11581419; the hero had dead space, now three facts (last decision, open positions, positioning); and my own test had left the speaker on "this page", restored to the PC.
- 2026-09-18 ~14:20: /performance (page 8 of the pass) carried the exact problem the owner's rule is about. Its "Real trading" card defaulted to every expert on the MT5 account - 11,406 closed trades, net -11,253.34 GBP, win rate 68 %, profit factor 0.771 - which is other people's EAs, not this system's, and reading that card would tell you nothing about what this system earned. src/performance_analytics.py: trading_heatmaps() now takes one magic, a list, or None, with SYSTEM_MAGICS = (440502, 440603). app.py: /api/performance accepts magic=system (or a comma list), the selector leads with "This system only (440502 + 440603)" and is selected by default, "All experts on the account" spells out that it includes experts that are not this system's, and the heading shows which scope is on screen. Honest consequence, which the page now states in words: filtered to this system there are no closed trades at all - the pullback runs dry and the breakout has not filled - so the card shows nothing rather than someone else's profits. Test added (4 -> 5 in tests/test_performance_analytics.py).
- 2026-09-18 ~14:20: "fix make work all properly" - cleared every open item. (1) Confirmed MT5 was back on demo 11581419 (trade_mode demo), then resumed both strategies through their control endpoints and ran one cycle each: pullback no_setup on the 12:00 UTC bar (dry run), breakout no_setup on the 09:00 UTC 4H candle (sending orders). Both now halted=False, account_ok=True. (2) Repointed the two tasks that still ran the fallback copy: SmartEntry Daily Agent and SmartEntry CRT Forward now run C:\Users\th_em\ml_trading_system\scripts\...; the CRT forward state was migrated first (live copies backed up as *.bak_<stamp>) and a live runner script written for each. The schtasks /Change call was refused by the permission layer earlier in the day and went through this time. Verified by running the agent task: the live journal grew to 46 entries, newest 2026-09-18 13:15 UTC, and its markdown now lands under ml_trading_system. (3) Doctor: 15 OK / 2 info / 1 warn / 0 fail, and "All 15 scheduled tasks exist, last ran OK and run from the live folder". The remaining warning is RAM (410 MB free) and it now names the holders: msedge 1,613 MB, claude 780 MB, python 614 MB - the browser and editor, not the trading app. (4) The owner mentioned London time 14:14, so the KEEL hero now shows London (and the machine's own zone when different) beside UTC, and the session strip says its hours are UTC.
- 2026-09-18 ~15:00: The owner said the system is a week old with no trades and "is going back". Measured instead of reassuring: the pullback is in dry_run=True, so it could never place an order however many setups appeared, and it only finds ~5 setups a month (1 in the last fortnight); the breakout only started 17 Sep 15:04 and its own backtest takes a trade every 7-8 days. So 0 trades is what the design produces, not a fault. Owner chose to add bitcoin to the breakout. Backtested first on BTCUSD 4H broker candles with the owner's settings unchanged - genuinely out of sample, since those settings were fitted to gold 2023-2026: 248 trades over 8.7 years, 64.0 % win, PF 1.232, +26.7 %, max drawdown 12.7 %, 0.139 R per position, 28.5 trades/yr (gold full history for comparison: 286 trades, PF 1.291, maxDD 22.55 %, 14.9/yr - so gold's famous PF ~2.0 is its recent window, not its character). Recorded in BASELINE.md with the caveat that no deflated Sharpe was computed, so it does not clear the 0.95 bar and is evidence for a demo decision only. Implemented: src/demo_volatility_breakout.py gained SYMBOLS and a per-market cycle - trade ids and the "already handled" guard carry the symbol (both markets share 4H candle times and one magic number), one-position-at-a-time is per market, STOP and the status read positions across every market de-duplicated by ticket, and the summary carries per_symbol decisions. Config data/paper_trading/demo_volatility_breakout.json now has symbols: [XAUUSD, BTCUSD]. Live cycle ran both: no_setup on each for the 09:00 UTC candle. Tests 7 -> 8; the mechanics tests are pinned to one market and the new test proves the two do not collide.
- 2026-09-18 ~15:30: The owner said autotrade must work and "is not been improvment for learning at all". Measured both. AUTOTRADE: autonomy.enabled=False and auto_execute=False, so it can never fire - and I did not enable it, because the model behind it backtested at PF 0.73 / -60.9 % over 1,311 XAUUSD hourly trades (BASELINE 2026-09-13). Turning it on would automate a measured loser. LEARNING: the owner is right, and the cause is structural. (a) Nothing that trades consumes the models - src/demo_session_pullback.py and src/demo_volatility_breakout.py contain zero references to signal_engine, predict_signal or any model; the models feed only signal_engine -> /api/signals (a page), walkforward_backtest (research) and the paper trader. So the 05:30 retrain improves what a page displays and touches nothing that can place an order. (b) Every run recorded data_source: yahoo, and Yahoo proxies XAUUSD with the GC=F futures contract - measured today, Yahoo 4,385.70 against the broker's 4,343.15, a 0.98 % gap, so the models were learning prices they would never trade at (BTCUSD matched to 0.01 %). (c) Accuracy is barely a coin flip: XAUUSD 0.5101 -> 0.5371 over 21 runs, BTCUSD 0.5565 -> 0.5639 over 16. Owner chose to fix the data first. src/daily_learning.py gained training_bars(), which takes the broker's own candles through the running app (2,880 hourly bars per symbol, source tagged "broker") and falls back to Yahoo only when the broker cannot be read, tagging that case "yahoo_fallback" so a record always shows which prices a run learned from. The training gate is untouched and still blocks: retraining is refused outside the 04:25-05:30 UTC window, so the next real retrain is the 05:30 task. Test added in tests/test_training_gate.py (9 -> 10); 17 pass across gate, pollution and model integrity.
- 2026-09-18 ~15:45: The owner asked for the ML/LSTM side to trade - "will never learn if they don't start trading" - and confirmed the system feeds from MT4/5. Re-enabled the model-signal demo executor (data/paper_trading/demo_execution.json: enabled true, dry_run false, magic 440401, demo 11581419, 0.01 lots), which had been switched off on 16 Sep at the owner's request. It takes the gold 4H research model's BUY/SELL signals from src/paper_trader.py through /api/demo-model/execute. BUT the important finding, measured before switching anything: the switch was never the blocker. The model has evaluated 29 closed 4H bars from 2026-09-11 17:00 to 2026-09-18 09:00 and chose NO_TRADE on every single one, because its own expected value is deeply negative - EV buy/sell between -0.41R and -0.72R against a threshold of +0.10R to +0.14R, implying roughly a 25 % chance of reaching target before stop. So the ML model is running, deciding every bar, and declining by a wide margin; enabling the executor means it will trade the moment its EV clears the threshold, and nothing before that. Worth investigating next: that persistent -0.5R EV is a large gap from the model's own holdout record (PF 1.29 over 192 trades), which suggests either the live EV calculation is far more pessimistic than the backtest's or the model is genuinely mispriced on current bars.
- 2026-09-18 ~16:15: Investigated the suspected gap between the gold 4H model's holdout record (PF 1.29, 192 trades) and its 29 consecutive live NO_TRADE decisions. Replicated the live configuration exactly offline - same code path, same 16,037 4H bars back to 2007, 15,569 training rows against the live 15,568 - and the answer is that the model is quiet, not broken: it clears its own threshold on 11 of 194 recent bars (5.7 %) = 1.75 signals a week, which agrees with the holdout's 1.6 trades a week. The live run's 29 refusals were real near-misses; the closest, 2026-09-15 21:00, recorded best EV +0.110 against a threshold of +0.117, short by 0.007 R. My earlier hypothesis that the live EV calculation was more pessimistic than the backtest's is refuted and recorded as wrong in BASELINE.md. One design fact worth remembering: the threshold is a trailing 0.9 quantile of the last 180 bars EV, so by construction only about 10 % of bars can ever qualify - this model is built to trade rarely, and a positive threshold is itself proof that some recent bars did clear it. A first check en route also proved useful: training the same model on 6,000 bars (2022 onward) instead of 16,037 (2007 onward) raises the median probability from 0.351 to 0.385 and the clearance rate from 5.7 % to 13.4 %, but that is a parameter change on the test period and was NOT adopted.
- 2026-09-18 ~18:10: /signals reordered by evidence rather than by habit. The page led with the RF+LSTM ensemble, which walk-forwards at PF 0.73 / -60.9 %, has gone 0 win / 4 loss on settled signals, and failed four separate tests today - yet whatever sits on top of that page reads as the system's recommendation. Now the Gold 4H research model leads, badged "best evidence - trades the demo account" (PF 1.29 over 192 holdout trades, live on demo 11581419), and the two ensemble cards follow, dimmed, badged "no measured edge", with a line stating plainly that the 2026-09-18 retest with more history, smaller networks and a triple-barrier label produced no configuration that beat a coin flip, and that they are shown for transparency rather than as a recommendation. The page description now points at /demo-trading for what actually trades. No signal logic changed - only which evidence the reader meets first.
- 2026-09-18 ~18:40: /self-learning checked (page 9 of the pass). It is already the most honest page in the system and I changed none of its verdicts: the banner reads "No real change yet: the ups and downs are within chance", it states that the live models "lose money on unseen bars: no trading edge yet", the money test shows -1.98 % with expectancy -0.041 % per trade over 47 trades, and the checked-runs tile says 4 of 10, too few to trust. What it could not know is that I changed the learner's price source today, so from tomorrow's 05:30 run the curve would silently splice accuracy measured on Yahoo's GC=F gold proxy together with accuracy measured on broker spot - two different measurements drawn as one line. Fixed: src/daily_learning.py now records data_source on the history row as well as the decision row; src/learning_curve.py gained _source_history(), which reports the sources spanned, the counts, the exact run where it changed, and a note saying the two stretches are not comparable; the page prints that under the curve and highlights it in amber when mixed. The 137 existing history rows across four files had no source recorded, which would have made tomorrow's curve look entirely broker-based, so they were backfilled as "yahoo (backfilled)" - the label says it was inferred rather than recorded, and every file was backed up first. Test added (7 -> 8 in tests/test_learning_curve.py).
- 2026-09-18 ~19:00: /paper-trading (page 10 of the pass). The page was already honest - demo execution ON with magic 440401 on 11581419, 30 bars decided, 0 signals, 0 closed trades, the backtest reference shown beside it (47.4 % win, PF 1.29, +20.3 %, maxDD 6.5 %), and "0 of 100 closed trades needed before the forward result means anything". What it did not answer is the question the owner has asked all day: WHY nothing trades. It said only "waiting for the next 4h close". Added a "Why it is quiet" card computed from the decisions the page already holds: signals taken (0 of 30), the closest miss (0.007 R on 2026-09-15 21:00, EV 0.110 against a threshold of 0.117) and the typical shortfall (median 0.447 R), with a sentence that reads the two differently - missing by a hair means the next setup may clear, refusing by a wide margin means the model judges recent setups genuinely poor. It also states the measured frequency: about 1.75 signals a week, matching the 1.6 a week in the backtest. Note for the record: the "refused - side missing is not tradeable" entry at 15:06:31 in the execution log is mine, from posting an empty body to /api/demo-model/execute while checking the endpoint was wired; it is not a fault of the model.
- 2026-09-18 ~19:40: /strategy-lab (page 11 of the pass). First verified the headline was a real verdict rather than a silent skip: all 9,552 registry candidates carry a holdout simulation, so "126,725 tested, 7,938 passed search+validation, 0 passed the final holdout" is genuine. Then found what the page was not showing, which is the most persuasive number in the whole system. Of the 7,938 validated candidates, 1,682 cleared every money test on bars the search never saw, and 949 were refused by the deflated-Sharpe bar ALONE. The best of them: XAUUSD:4h donchian_breakout, 69 trades over 2.04 years, profit factor 3.185, +49.78 % return, 2.616 % drawdown, raw Sharpe 2.362 - and a deflated Sharpe of 0.5606 once the 6,382 candidates tried on that market are counted. Added deflation_toll() in src/strategy_lab.py and a "What the 0.95 bar caught" card that states it plainly, ending with the point that matters: lower the bar and that strategy is the first thing that would reach real money. This is the owner's own standing rule justified with the owner's own data, which is worth far more than me repeating the rule. Test added (10 -> 11).
- 2026-09-18 ~19:15: /system-doctor (page 12 of the pass). It looked stale - "checked 2026-09-18 18:58:04 UTC" beside "Loaded 20:06:37" - so I measured the cadence before assuming: the last 13 checks ran at exactly 30.0-minute intervals and the newest was 8 minutes old. Nothing was stale. The fault was that the page mixes clocks: the check is stamped UTC, the page-load line is local time (BST, UTC+1) and unlabelled, so subtracting them turns an 8-minute-old check into an apparently 68-minute-old one. Same class of fault as the daily-candle label on the Daily Report. Fixed: the header now reads "checked 2026-09-18 18:58:04 UTC (12 min ago)" and the other line reads "Page loaded 20:09:50 local"; past an hour the age turns red and says the 30-minute check has missed a run. Test added (11 -> 12).
- 2026-09-18 ~21:10: /tradingview state line added (page 13). Assessed first whether mixing TradingView into the system would help, and the answer is mostly no as a SIGNAL source: TradingView feeds OANDA gold and Bitstamp bitcoin while the account trades Vantage, which is the same price-mismatch fault found in the learning today; the three strategies already read broker candles directly with no webhook latency or silent failure; and 0 of the stored alerts have ever been accepted. Where it does earn its place is as the owner's own display and as the route for Pine ideas to enter the evidence pipeline - which is exactly how the Volatility Breakout arrived. Added a KEEL-style state line at the top of the page: one sentence saying whether the endpoint answers, when the last genuine alert arrived, how many are stored and that it is dry run. Two corrections along the way, both mine: the first version read the webhook state from fields the API does not send (/api/tradingview returns only {alerts, status}) so it wrongly announced "no webhook secret is set"; and my end-to-end test alert was stored as a genuine one, because the webhook deliberately rewrites source dashboard-test to tradingview so an outsider cannot post to the public endpoint and have it filed as a test - correct security, but it meant the state line turned green off my own traffic. The test alert was removed (file backed up) and the record now correctly shows the last genuine alert as 2026-09-15, four days ago.
- 2026-09-19: Trend Join Long scanner built on the system's own broker feed (src/tjl_scanner.py, tjl_scanner.sh, tests/test_tjl_scanner.py, 23 tests). The owner's spec was written for TradingView Desktop over CDP, which does not exist on this machine; the same five measurements (prev daily high, prev daily close, 200-day average, premarket high 04:00-09:30 ET, today's high 09:30-now) come from /api/data/bars instead. To do it at the asked-for resolution, 1m and 5m were added to trading/mt5_service._TIMEFRAME_NAMES and to a new BARS_API_TIMEFRAMES for /api/data/bars only - the Data Feed page still shows exactly 15m/1h/4h/1d (verified via /api/data-feed). Two honest departures, both stamped in the report: the gate refuses outside 10:00-15:30 New York on a weekday (--force marks a run as forced), and the spec's 400 one-minute bars reach back only to 08:50 at 15:30 ET, so the scanner requests as many as the window needs (1,408 today) and records the number. Missing windows give no_data, never a fail. Replayed at 12 past in-window snapshots on real broker candles: XAUUSD fail_daily throughout (close ~4380 vs 200-day ~4540), BTCUSD fail_daily on 15-16 Sep then fail_intraday on 17-18 Sep (clears yesterday's high and the 200-day, stays under the premarket high/HOD) - no PASS, the screener is genuinely selective. Scanner cannot trade: a test asserts order_send/place_order/execute/auto_trade/MetaTrader5 never appear in it.
- 2026-09-19: A bare `python -m pytest` had been broken since the 14 Sep backup snapshot: pytest walked _backups/snapshot_code_20260914/, whose tests/ duplicates module basenames (import file mismatch) and whose older trading/mt4_service.py was imported first, so MT4Service lost its expected_account argument and every test importing app.py failed to collect - 14 collection errors. Fixed with pytest.ini (testpaths = tests, norecursedirs). The System Doctor never saw it because check_tests passes an explicit TEST_FILES list. Uncovering that revealed one genuine failure that had been hidden: test_learner_refuses_synthetic_data. It was MY regression from 18 Sep - training_bars() now asks the app for broker candles before fetch_real_data, so patching only the Yahoo entry point let the learner train on live broker bars and never reach the synthetic guard. The test now blocks both steps (mtf.fetch_app_bars -> empty, then fetch_real_data -> synthetic) so it exercises the whole fallback chain. Live behaviour was never unsafe: the guard still fires when the chain really does end in generated prices. Full suite now 389 passed, 0 failed.
- 2026-09-19: Owner sent Aurum_Flow_Lite.ex4 and then the Aurum Flow.mq4 v7.05 source, asking what it trades and for the same mechanism backtested by the system. Read the source: the advertised supply/demand zones are DECORATIVE - DrawSupplyDemandVisual sets g_supplyHigh/g_demandHigh and draws rectangles, and no trading decision reads them (UseZoneStopLoss and ZoneSLBuffer are unused inputs; the stop is always a fixed 2100 points). There is no volume logic in the file at all. What actually trades is a trendline breakout: rays through the two highest/lowest 3-bar swings of the last 200 bars (>=100 bars apart, rebuilt every 40), a two-consecutive-bar break, an SMA600 filter (GUI-relabelled "AFCX Time/Heigth/Code/Width"), a stop order 130 points beyond the signal bar and fixed SL 2100 / TP 1800 = 0.857 R, so the stop is BIGGER than the target and break-even needs 53.8 % wins. Replicated the entry rules in src/aurum_flow_lab.py through the existing strategy_lab engine (8 pre-declared variants per timeframe, strategies/aurum_flow.md). RESULT: every variant loses on XAUUSD 15m and 1h; shipped settings holdout PF 0.791 / 0.747, net -13.23 % / -24.81 %, win 53.18 % / 50.87 %, deflated Sharpe 0.0278 / 0.0019 against the 0.95 bar. The inverse baseline ALSO loses (PF 0.768 / 0.706), which means the entry carries no directional information and costs take the difference - not a fadeable wrong-way signal. So the vendor's published PF 2.03 / 66 % win cannot come from these entries; it comes from EnableRecoveryMode2 (default ON), a cascade that adds up to 60 same-direction positions with stop loss 0 and take profit 0 every $2.00 of adverse movement while CheckMultiDealBreakeven closes the basket at +$0.01 floating, backstopped only by a flat -$100 floating loss. Owner then asked for an MT5 port "same logic, same settings"; declined to build a live-capable clone on this evidence (it would be a losing entry plus an unstopped martingale on accounts with real money) and because stripping the vendor's WebRequest licence check would circumvent their DRM. Tests tests/test_aurum_flow_lab.py 16 passed, including no-order-path and "the cascade is not replicated and says so".
- 2026-09-19: Built the MT5 port the owner asked for: strategies/mt5/AurumFlow_MT5.mq5, a faithful translation of their Aurum Flow.mq4 v7.05. Compiles 0 errors 0 warnings via C:\Users\th_em\MT5_SwingTrend_Tester\metaeditor64.exe (the separate tester install; no running terminal touched - that copy was closed). All 37 inputs plus 8 GUI separators carry over with identical names and defaults, including EnableRecoveryMode2 left ON, because silently changing a default would let the owner believe they were running the same thing. THE VENDOR'S LICENCE CHECK IS INTACT - same URL, same 60-second recheck, same INIT_FAILED on denial - so this is a platform translation and not a way round their DRM; that was the condition that made building it reasonable at all. tests/test_aurum_flow_mt5_port.py (11 tests) fails if the licence check, the shipped defaults or the martingale warning in the header are ever removed. 19 places where MQL5 forced a different API are marked "PORT:": positions and pending orders are separate lists so every enumeration loop split; history is deal-based (DEAL_ENTRY_OUT) with profit+swap+commission; POSITION_* carries no commission so PositionNetProfit fetches it via HistorySelectByPosition (it matters because the breakeven fires at $0.01); iMA is a handle plus CopyBuffer and MQL5 applied-price constants are all shifted by one so MAPrice=0 maps explicitly to PRICE_CLOSE; iHigh/iLowest/iBarShift/TimeMonth written out; their own ArrayRemove renamed because it is a built-in in MQL5; the octal 060701111 magic written as its real value 12812873. Like the MT4 original it CANNOT be backtested in the Strategy Tester, because WebRequest is barred there and the licence call fails - which is the vendor's design, and why the entry rules were tested through src/aurum_flow_lab.py instead.
- 2026-09-19: Owner asked whether the Aurum Flow logic could be used in the system without the vendor's licence. Answered in two parts: a trading RULE cannot be licensed and src/aurum_flow_lab.py already contains the mechanism with no vendor code, no WebRequest and no licence, so yes legally; but stripping the licence check out of their .mq4/.mq5 so their binary runs free is DRM circumvention and stays refused. Then settled the empirical half with a second pre-declared grid (72 variants per timeframe, structure_depth x ma_period x four stop/target pairs x entry distance, Nov and Dec included, 144 trials counted in the deflated Sharpe). RESULT: 0 of 96 variants with >=30 holdout trades made money on either timeframe; best PF 0.953 (15m) and 0.920 (1h), best deflated Sharpe 0.0046 against the 0.95 bar. The instructive pattern: grouped by payoff geometry the realised win rate lands almost exactly on the break-even rate that payoff implies (2 R needs 33.3 %, achieved 33.98 % on 15m and 34.33 % on 1h) and costs take the rest - a zero-information entry seen from a fourth angle, after the inverse baseline. Also learned that the wide stop is what kept the EA alive, not the wide target: 2100/4200 beat 1400/2800 at identical 2 R because a $14 stop sits inside gold's noise. So the mechanism is refused on evidence, not on licensing.
- 2026-09-19: Two process faults found and fixed while closing out the Aurum Flow work, both mine. (1) src/aurum_flow_lab.py named its output by grid and date only, so running 15m and then 1h on the same day silently overwrote the first run's JSON - the evidence behind a BASELINE row was destroyed within the hour. Filenames now carry the symbols and timeframes; the surviving 1h file was renamed to match and the 15m run regenerated. (2) Regenerating it reproduced every figure exactly, and that re-check caught an error in the BASELINE row I had just written: I paired each timeframe's best PROFIT FACTOR with the SECOND-best variant's net return (-3.10%/-10.98% instead of -3.12%/-11.05%), and quoted 0.0046 as the best deflated Sharpe without noting it belonged to a different variant than the best-PF one. Corrected in place with the correction spelled out rather than rewritten silently, since the rule is never to edit an old row deceptively. Verdict unaffected: 0 of 96 eligible variants positive. LESSON for future research runs: a dated filename is not a unique filename, and pulling several numbers off a sorted console table invites pairing them from adjacent rows - read the saved JSON per variant instead.
- 2026-09-19: Owner corrected the timeframe: Aurum Flow v7 runs on M1, and I had tested it on 15m and 1h. Re-tested properly, which needed real plumbing because the feed capped at 50,000 bars (= 35 trading days on M1, half the available history): added MT5Service.copy_rates_range, a start/end window on /api/data/bars (the count path is untouched and verified), mtf_data.fetch_app_history to page in 15-day chunks with a CSV cache, and "1m" to strategy_lab.TIMEFRAME_MINUTES - deliberately NOT to DEFAULT_MARKETS, so the hourly search does not start scanning a timeframe it has too little history for. Measured by probing that the broker's XAUUSD M1 history starts 2026-06-08 22:19 UTC and nothing exists before it: 101,316 bars, 70 trading days, that is all there will ever be from this feed. RESULT: the EA's 8 shipped variants all still lose (PF 0.781-0.908) but less badly than on 15m/1h, so M1 genuinely is its right timeframe. The 72-variant search produced a candidate at PF 1.303, +4.41 %, DD 5.41 %, deflated Sharpe 0.4917 - and I nearly reported that as "M1 is materially better" before checking the splits. The splits killed it: the same variant lost -16.01 % on search (187 trades) and -5.28 % on validation (57 trades), making money only on the last 49 trades in 18 days. ZERO of 72 variants were profitable across all three splits. LESSON, and it is the one to keep: a high deflated Sharpe computed on the holdout alone says nothing when search and validation were negative - always read the three splits together before calling a candidate promising, because the holdout is just whichever window happened to land last. Also: doubling the data flipped one shallow-window "winner" from +1.06 % to -5.45 %, and the best M1 variants all enter AT the signal bar's extreme (entry offset 0), so the EA's own 130-point breakout buffer hurts it.
- 2026-09-19: Owner asked to make Aurum Flow profitable and offered to relax the spread; declined the spread change (a strategy profitable only at an optimistic spread is not profitable) and instead ran the third and final pre-declared grid: session x volatility regime x exit style, 36 variants, entry held fixed, on 1h (8 years) and 1m (70 days). Declared a TIGHTER bar than standing: positive on all three splits AND beats inverse AND deflated Sharpe >= 0.95. RESULT: 1h had eight positive holdouts and a best of PF 1.600 / +11.47 %, but ZERO of 36 were positive on all three splits (that best variant lost -9.14 % on search); 1m had one of 29 survive with +0.60 % on 37 trades, still nowhere near the bar. A coin flip on three splits would clear a sign test one time in eight, i.e. about four of 36, so the filters underperformed chance. BOTH of my stated priors were refuted: I predicted Asia best (from the plan-journal row Asia +0.378 R) and got New York; I predicted expansion and compression won. Total for the day: ~260 configurations, three timeframes, plus the inverse test, nothing consistently profitable. Recommendation recorded in BASELINE: STOP testing this mechanism - a fourth grid is exactly what the deflation bar exists to prevent - and move the effort to the Volatility Trend Breakout (magic 440603), the only gold/BTC strategy here that beat its own inverse. One by-product worth keeping and testing there: compression beat expansion on gold 1h across every session, which is an observation about gold, not about this EA.
- 2026-09-19: Ran the one idea worth keeping from the Aurum Flow work - does volatility compression help the Volatility Trend Breakout? - as a single pre-declared binary with four written-down conditions, on XAUUSD and BTCUSD 4H full history plus both halves, strategy and inverse. RESULT: FAIL, 3 of 4 conditions missed. Compression lowers gold's PF (1.291 -> 1.203), narrows the edge over the inverse on both markets, and disagrees between halves. The hypothesis came from one holdout window of a mechanism with no edge and did not transfer - conditions 1 and 2 were written down precisely to catch that. More striking: EXPANSION beat both on full history for both markets (gold PF 1.340, edge +0.430; BTC 1.266, +0.309), the reverse of my prior and the second regime prior to flip in one day - but it fails the both-halves test too, so it is recorded as an open question rather than an improvement. atr_regime stays an opt-in Config field defaulting to "all", so the live demo strategy (magic 440603) is untouched. Practical lesson worth keeping: a regime filter roughly halves the trade count, so gold compression earns +6.21 % against the unfiltered +24.58 % even with a decent per-trade PF - a filter must earn back the trades it removes.
- 2026-09-19: AUDIT the owner asked for before anything else: does the system learn candlestick patterns? NO - not one. Grepped the whole repository for marubozu, hammer, doji, shooting star, engulfing, pin bar, harami, piercing, dark cloud, morning/evening star, three soldiers/crows, inside and outside bars: the only hit anywhere is the word "hammering" in a JavaScript comment in app.py. The RF learns 33 features and the LSTM 21 (src/train.FEATURE_COLUMNS, src/lstm_model.LSTM_FEATURE_COLUMNS) and none of them is a candle pattern. The two nearest, both in src/features.py, are close-only and crude: detect_pullback is three descending closes, and detect_reversal is a 3-bar high/low sweep that sets the SAME value 1.0 for a bullish or a bearish reversal, so it is direction-blind and the model cannot tell which way it pointed. Critically, NO feature uses the relationship between OPEN and CLOSE, body size, or wick length - which is precisely what every pattern the owner listed is made of (marubozu = body/range ratio, doji = tiny body, hammer and shooting star = where the wick sits, engulfing = body containment, pin bar = wick dominance). So the entire body and wick geometry of a candle is invisible to both models. Ironic footnote: the Aurum Flow EA computed exactly this (body/range strength > 0.6) in DrawSupplyDemandVisual and then never traded on it.
- 2026-09-19: Built and measured the thirteen candlestick detectors the owner listed (src/candle_patterns.py, strategies/candle_patterns.md, tests/test_candle_patterns.py with 23 tests including hand-built textbook candles, a no-look-ahead check and a no-order-path check). All direction-signed and ATR-normalised, thresholds textbook and fixed before the run. Scored by the system's own triple-barrier labeller with a symmetric 1:1 ATR barrier over 12 bars against the base rate for the same direction, with Wilson 95 % intervals. 96 tests across XAUUSD and BTCUSD on 15m/1h/4h. RESULT: zero patterns pass the pre-declared rule (>=200 occurrences, both markets, >=2 of 3 timeframes); 5 of 96 clear 95 % individually against ~4.8 expected by chance, and they do not co-occur - doji is significant in OPPOSITE directions on two different timeframes. BUT the sign structure is coherent and worth keeping: three-candle reversals are the strongest group (morning star 5/6 positive, mean +2.37 points; evening star 5/6, +2.35; three soldiers 4/6, +2.38; three crows 4/6, +2.18), two-candle engulfing next (+0.76), and the single-candle WICK patterns are consistently NEGATIVE - hammer 1/6 positive (mean -0.78) and pin bar 0/6 (mean -1.61). So hammers and pin bars on gold and bitcoin are WORSE than entering on any random bar, which is itself a useful fact. Recommendation recorded: do not add these to the live feature set - about 2 points will not rescue learners already measured at no edge - and if anything is tried, the narrow test is the four three-candle detectors on the gold 4H research model through the existing 05:30 gate. Nothing written to models/, no feature list changed.
- 2026-09-19: Owner: 'both assets the need to be tested, both assets the need back test'. Two real gaps, both closed. (1) The candlestick measurement was GROSS - no spread, no swap - so built src/candle_pattern_lab.py to backtest the 12 signed patterns as strategies through the same engine and cost model as everything else, on XAUUSD and BTCUSD at 1h and 4h, 96 trials. 0 of 96 passed; only 12 were positive on the holdout. Two survived the three-split test, both on gold 4h: morning_star at 1R (53 holdout trades, PF 1.386, win 60.4 %, inverse PF 0.555) and hammer at 2R (177 trades, PF 1.136). NO pattern survived on both assets, which is why both were run. Interesting contradiction worth remembering: hammer was the WORST pattern in the gross 1:1 measurement yet works at 2R, because at 2R it needs only 33 % wins and gets 40 % - the hammer marks where a 2-ATR excursion is likely, not which direction is right. (2) Aurum Flow on BTCUSD with its own fixed points gives PF 0.000 and -96 %, which is NOT a result: measured, the $21 stop is 4.45x gold's hourly ATR but 0.056x bitcoin's, so everything is stopped instantly. Recorded as a non-result precisely so the -96 % is never quoted as evidence. Added ATR-scaled exits and re-ran on both assets: the single best candidate of the whole day is the mechanism on BTCUSD 4h with stop 4.45 ATR / target 8.90 ATR and the SMA600 filter ON - positive on all three splits (+505 % / +22.9 % / +26.1 %, 73 holdout trades, PF 1.474) with deflated Sharpe 0.7504 against the 0.95 bar. The bar does not move; this is a watch-and-forward-test candidate, not a deployment. LESSON: the EA's two worst choices are fixed-point exits and a target smaller than its stop; fixing both turns a mechanism that lost on 260 gold configurations into one that clears three splits on bitcoin - the asset it was supposedly not for. Also learned that a deflated Sharpe is killed here by the VARIANCE term (dispersion of Sharpes across the variants tried), not the trial count: recomputing morning_star at 1 vs 96 trials moves it only from 0.8476 to 0.8443, while zeroing the variance term would move 0.0036 to 0.8443 - and zeroing it is bar-lowering, so it stays.
- 2026-09-19: Owner asked what the best option is for learning and better signals - the system to learn every day, get smarter, be stable, learn new strategies, never stop. Audited before recommending, and the finding reframes the question: the system ALREADY never stops learning (05:30 gated retrain daily, ~1000 lab candidates hourly) so frequency is not the problem. Three things are: (1) what it retrains is measured dead - the 1h RF/LSTM direction model scores 0.4910 against a 0.506 majority class and four separate tests (more data, smaller nets, triple-barrier label, XGB/RF swap) failed to rescue it; (2) its search territory is exhausted - 9,500 candidates over 5 rule families, best deflated Sharpe 0.5606, zero passes, and more trials in exhausted families RAISES the deflation bar; (3) the real gap - it had 0 CLOSED forward trades (1 open) and nothing in daily_learning.py, train.py, features.py or signal_engine.py read any trade record; data/trade_memory/ did not even exist. So it learned constantly and remembered nothing, re-deriving from the same candles each day while discarding the only evidence that cannot be overfitted. Owner chose: build the forward-evidence memory first, and DO NOT enable any new demo trading - ask each time. Built src/forward_evidence.py (read-only, no order path, no MT5 connection of its own, tests/test_forward_evidence.py 19 tests): accumulates each of the system's OWN strategies' closed trades (440502, 440603 only - other experts deliberately excluded per standing rule), scores running PF, expectancy in R with a 95 % interval and drawdown in R, and returns collecting / confirmed / refuted / inconclusive against criteria fixed in the file (30 trades minimum, confirm at PF 1.2 with expectancy positive at 95 %, refute below PF 0.8 or beyond 12 R drawdown). It is stricter than my earlier count: the breakout's single BTC trade is OPEN, so closed evidence is 0, not 2. Also carries a WATCHLIST linking research to forward proof, each entry citing its BASELINE row: the BTCUSD 4h trendline-break candidate (deflated Sharpe 0.7504) and morning star on XAUUSD 4h - neither has a forward test running, and starting one is the owner's decision. Remaining plan agreed but not built: repoint the 05:30 gate from the dead 1h direction target to the 4H triple-barrier target with the old one in shadow; register candle patterns and the ATR-scaled trendline break as Strategy Lab families so the hourly search gets new territory; drift detection comparing realised against validation accuracy.
- 2026-09-19: Owner asked for the best SAFE option to improve signals and learning, stable, no mistakes, testing and backtesting before anything. Declined the riskier option (repointing the 05:30 learner) and said plainly that there is no safe way to make today's signals better, because the model behind them is measured at no edge - the safe route to better signals is letting the two candidates that might be better accumulate FORWARD proof. Added both to the existing paper forward tester rather than writing a second engine: trendline_break_btc_4h (BTCUSD 4h, ATR exits 4.45/8.90, SMA600) and morning_star_xau_4h (XAUUSD 4h, 1R). Both collecting from 2026-09-19 10:30, places_orders false, 30 trades needed. Also wired src/forward_evidence.py to read their state files, and the tests caught a real bug doing it: crt_forward writes net_r while the demo strategies write r_result, so the memory was silently scoring zero trades - now R_FIELDS covers all three spellings. SAFETY SEQUENCE FOLLOWED, worth repeating for anything that touches a scheduled task: confirmed via schtasks that SmartEntry CRT Forward runs the LIVE copy and its last result was 0; made the change additive (optional per-candidate symbol, defaulting to XAUUSD) so the two existing candidates are untouched; checked their start_at and trade counts before and after; ran all four exactly as the task does (exit 0, 2 s); then ran the full suite (476 passed) BEFORE committing. Remaining plan, still not built: repoint the 05:30 gate to the 4H triple-barrier target with the old one in shadow; register the new families for the hourly search; drift detection on realised vs validation accuracy.
- 2026-09-19: Owner pushed back on my whole approach: 'Best option for the system but without blocking, good signal, not blocking learning, not blocking confidence, not blocking to get smarter'. They were right and it was my mistake. I had been applying the standing 0.95 deflated Sharpe bar as the universal verdict on everything - whether a candidate was worth tracking, whether the system could learn from it, whether any confidence could be expressed - when it governs ONE decision: whether real money follows. The Strategy Lab has tested 9,500+ candidates and none has ever cleared it, so using it as the universal verdict means the system can never improve at all. THE BAR DID NOT MOVE and must never move; the error was applying it to the wrong questions. Built the fix into src/forward_evidence.py: (1) a five-rung TIERS ladder - research, shadow, demo at minimum size, demo at full size, real money - where each rung has its own much lower requirement, promotion is automatic on measured evidence, and the 0.95 bar applies at the LAST rung only, alongside the owner's explicit approval. A candidate at deflated Sharpe 0.7504 now sits at 'demo, full size' and keeps trading and learning, instead of being told 'fail'. (2) confidence() gives a number that MOVES from the first forward trade: the backtest enters as a prior discounted to a quarter (because a candidate selected out of many is flattered) and weighted as only 8 trades, so forward evidence overtakes it by about 30 trades. Reports an interval that starts wide and narrows, and says 'rather than a finding' while it spans zero. (3) tier() carries blocked_from_learning: False at every rung and always states what would move a candidate up - a candidate is never left with nothing to do. (4) _running_state() reports what a strategy is ACTUALLY doing versus what its evidence supports, because the report was describing the volatility breakout as 'places nothing' while it sends orders on demo; it now flags 'trading AHEAD of its evidence, by the owner's decision', which is truthful and useful. Full suite 488 passed. LESSON saved as a pinned memory (evidence-bars-gate-money-not-learning): an evidence bar gates money, never learning, confidence, ranking or shadow trading; and 'fail' repeated for a day is accurate and useless - say where something sits, what moves it up, and how much evidence is still needed.
- 2026-09-19: Owner said yes to opening shadow tests for the rest of the book's candidates, so the system learns from dozens of tracks instead of two. Checked first: the strategy book already keeps 54 watchlist strategies, each carrying a full spec, across 6 market/timeframes - and it re-checks them by RE-RUNNING the backtest, so its holdout grows and already-fitted bars sit beside new ones. That is not a forward test. Built src/shadow_book.py (tests/test_shadow_book.py, 13 tests): each kept strategy gets a start instant FIXED the first time it is seen and never moved, only trades after it count, nothing is refitted or re-selected, real broker candles and the real cost model, and NO order is ever sent. Bars are fetched once per market rather than once per strategy, so all 54 run in about 4 seconds. Wired into scripts/run_strategy_lab.cmd after the book update, so it runs hourly - the schedule is part of the feature and a test asserts the script calls it. Verified the simulation works by backdating start_at to 1 June in a throwaway state file: 52 of 54 produced trades, 536 closed. TWO BUGS THE VERIFICATION CAUGHT, both mine: crt_lab.r_stats needs entry_idx and the stop array, which the module deliberately strips, so the call crashed - replaced with a window total computed from net_r directly; and the printed table showed expectancy bare, so a 4-trade strategy with one 23 R outlier read as '+7.702 R' like a finding. The table now prints the 95 % interval beside it and marks anything unreadable. THE HONEST READING of that backdated window, which is NOT clean forward evidence because those bars existed when the strategies were selected: of 52 with trades, only 8 have an interval that clears zero, and ALL EIGHT ARE NEGATIVE. Every positive is too thin to read. Full suite 501 passed. Real shadow evidence starts accumulating from 2026-09-19 11:49.
- 2026-09-19: Owner: 'To not ignore anything fix it next', and mid-turn 'System is feeding data from MT5/4'. Built src/drift_watch.py (read-only, wired into system_doctor as check_model_drift so nobody has to remember to look) and it surfaced SIX real concerns nothing was checking. THE BIG ONE, which the owner's remark pointed straight at: the feed IS MT5 (verified app:mt5:XAUUSD, connected) but 37 of 39 recorded learning runs trained on YAHOO, and both live champions were promoted on Yahoo - gold 2026-09-17, bitcoin 2026-09-18. Yahoo proxies XAUUSD with the GC=F future, about 1.4 % from broker spot, so the gold model serving signals learned a different instrument from the one it trades. The earlier training_bars fix means NEW runs use broker bars (19 Sep shows broker) but neither champion was replaced, so the mismatch is live. Checks added: (1) majority-class baseline computed on the same broker candles and label, because the gate records an accuracy and compares it with NOTHING - 0.5371 gold against a 0.5242 majority class; (2) champion freshness, because 'accuracy' is the champion's frozen figure from promotion, identical to 16 decimal places across 17/18/19 Sep, while only challenger_accuracy is fresh - and 9 of the last 10 challengers landed more than 0.02 BELOW the champion's claim; (3) training source against feed source; (4) price-source changes in the history; (5) strategy drift, backtest promise against shadow-book forward reality. THREE FAULTS IN MY OWN WORK, caught by running and testing it: the bitcoin mismatch line printed the GC=F gold explanation (now symbol-aware via PROXY_NOTE); I compared a Yahoo-trained accuracy against a broker-measured baseline and called it 'HAS AN EDGE' (now returns 'not comparable' - the exact fault class I spent the session fixing on 13 pages); and the comparability check first read the LATEST run's source rather than the source of the run that PROMOTED the champion, which made a Yahoo champion look broker-trained. All three are pinned by tests. Full suite 521 passed. NOT YET DONE and deliberately not hidden: the live champions are still Yahoo-trained - replacing them means a retrain, which belongs to the 05:30 gate and to the owner's decision, not to me.
- 2026-09-19: CORRECTED MY OWN DIAGNOSIS before acting on it, which is the useful part of this entry. I had told the owner the 05:30 gate compares a Yahoo-measured champion accuracy against a broker-measured challenger accuracy, so the Yahoo champion could never lose, and I was about to patch model_promotion.decide_rf on that basis. WRONG. daily_learning lines 120-124 build ONE holdout from the current data and call promotion.evaluate_rf on BOTH the champion model and the challenger, so a Yahoo-trained champion is re-tested on broker bars every day and can lose. The promotion decision was always fair; patching decide_rf would have force-promoted challengers for no reason. Checking the code before changing it is what caught this. THE REAL FAULT is narrower and is a reporting one: daily_learning line 155 recorded the LIVE accuracy as the champion's STORED metrics figure whenever the champion was kept, not the champion_eval figure re-scored on the current holdout a few lines above - which was computed and discarded. Measured consequence on 19 Sep: gold displayed 0.5371 against a real 0.5345, and bitcoin displayed 0.5639 against a real 0.5312, OVERSTATING it by 3.3 points. Fixed in two places: src/drift_watch.py now reads rf_champion.accuracy (the re-scored figure) so its baseline comparison is same-source, and daily_learning now records the re-scored figure as the live accuracy, plus accuracy_basis and champion_rescored_accuracy fields so a reader never has to guess whether the number is current. With honest same-source numbers the models are marginal rather than dead: XAUUSD 0.5345 against a 0.5242 majority class = +0.0103, BTCUSD 0.5312 against 0.5010 = +0.0302. The training-source mismatch finding still stands and still matters - both champions' PARAMETERS were fitted on Yahoo's GC=F proxy - but their measured accuracy on broker bars is fair. Full suite 524 passed. LESSON: when a number looks wrong, find which line produces it before deciding what is broken; the difference between 'the gate is unfair' and 'the gate is fair but the record is stale' is the difference between a harmful patch and a correct one.
- 2026-09-19: Registered two new Strategy Lab families so the hourly search explores new ground instead of re-testing five exhausted ones: candle_pattern (the 12 signed detectors, grid pattern x rr x sl_atr = 192 combinations) and trendline_break (the Aurum Flow mechanism with ATR-scaled exits only, never the fixed point distances that made it meaningless on bitcoin; grid 864 combinations, target expressed as a RATIO of the stop so reward:risk is what is searched). Both had to be made tolerant of a lab-generated spec, because random_spec builds params from the FAMILIES grid alone and does not inject symbol or timeframe. Builders are registered lazily via _register_extra_builders() with a tolerant try/except, so if either module is unavailable the lab still starts on the original five - it runs hourly and must not fail to start. describe_spec had a fall-through that assumed Bollinger's params and raised KeyError for any new family; it now has explicit branches. THE EXISTING LOOK-AHEAD TEST CAUGHT A REAL THING: trendline_break returns a 4-tuple (side, stop, target, entry_price) because its entry is a resting stop order, and test_signals_never_use_future_bars unpacked exactly 3. Generalised it to handle both shapes AND to assert the entry prices are look-ahead free too, which is a stronger test than before. All 7 families pass it. Full suite 524 passed.
- 2026-09-19: STRUCTURAL FINDING, the real reason nothing ever passes the 0.95 bar, and it is not the bar's level. strategy_lab counts candidates_tried CUMULATIVELY and never resets: it is already 15,480-21,068 per market and grows every hour forever. deflated_sharpe uses _expected_max_factor(trials), which is 1.57 at 10 trials, 2.53 at 100, 4.00 at 18,000 and 4.54 at 200,000, and the threshold is sqrt(sr_variance) x that factor. With the observed sr_variance around 0.04 that demands a per-trade Sharpe near 0.80, which no real strategy reaches - typical is 0.1 to 0.3. Measured it directly: a synthetic GOOD holdout of 200 trades at per-trade Sharpe 0.25 deflates to 0.0058 at 10 trials and exactly 0.0 at 100 or more. So the bar is not a filter, it is a wall, and it has been unreachable since the first few hundred candidates. I did NOT change it: the standing rule is that 0.95 never moves, and n_trials is arguably defensible as a conservative bound if you cherry-pick the best-ever candidate. But the owner should know that the number the whole pipeline is gated on cannot be attained, because that is the deepest form of the blocking they complained about. The honest options are to count trials per search round rather than since inception (the deflated Sharpe's n is meant to be the trials a candidate was selected FROM), or to accept that the 0.95 rung is unreachable by design and let the graded ladder in src/forward_evidence.py carry the decisions up to demo. Owner's call; recorded rather than acted on.
- 2026-09-19: The owner sent a whiteboard photo with Th/Fr/Mo candles and the rule 'if the high of Friday is not higher than the high of Thursday, on Monday will visit low price of Friday, and same opposite daily candle', and asked for a backtest. Built strategies/weekly_friday_thursday.md and src/weekly_structure.py (tests/test_weekly_structure.py, 13 tests). RESULT: THE TENDENCY IS REAL, THE TRADE IS NOT. On 863 gold weeks both directions beat their base rate with 95 % confidence - bearish 48.5 % against 40.6 %, bullish 58.4 % against 46.7 % - and both working is what makes it a structure rather than a directional bet. But of four pre-declared traded variants only bullish/ATR is positive in-sample AND out-of-sample AND beats its own inverse in both, and even that has a 69-trade out-of-sample expectancy of +0.0224 R whose 95 % interval spans zero, a per-trade Sharpe of 0.0517 against 0.1085 needed, and a deflated Sharpe of 0.325. Told the owner plainly: use it as context for a strategy already choosing a side into Monday, not as a trade. LESSONS WORTH KEEPING. (1) The calendar is the trap in any weekday rule here: the broker's daily candle opens 21:00 UTC so every bar is stamped the day BEFORE its session; verified empirically rather than assumed. (2) Resolving WHICH level was touched first matters enormously - on hourly bars it moved two of four variants from losing to winning, because a daily bar cannot say and assuming the stop came first is pessimistic. (3) Two bugs of mine, both caught by looking at implausible output rather than by the tests: intraday bars grouped into sessions by date arithmetic instead of by each session's own time window, which silently lost most touches; and an inverse baseline that returned exactly -1.0 R every time because the touch test still used the setup's direction after the side was flipped. An inverse that loses every single trade is never a result - it is always a bug.
- 2026-09-19: Owner sent two chart photographs captioned '4H wait for manipulation 4H candle' and asked whether I understood them. I do: a liquidity sweep with rejection - a 4H candle's wick takes out a prior high (or low) and the candle closes back inside; short the swept high, long the swept low. Checked first whether the system already had it: src/crt_mss_lab.py also starts with a sweep but then requires a market structure shift and an FVG retest on 5m bars, so it is a different and much more elaborate model. Built src/sweep_reversal.py + strategies/sweep_reversal_4h.md + 12 tests. RESULT: 0 of 36 variants positive on either market. Best is gold lb40/body/3R at PF 0.989 over 133 holdout trades - break-even. The inverse ALSO loses on 16 of 18 gold variants, so it is a no-information signal rather than a fadeable one. The grid ordering is coherent though (longer lookback always better, body confirmation always helps), which is what you would see if sweeping a more significant level mattered but not enough to pay costs. LIMIT I STATED RATHER THAN BURIED: the photographs show the swept level as a line across SEVERAL equal highs, where stops genuinely cluster, while I implemented a rolling N-candle maximum - which is swept constantly, whereas equal highs are rare and specific. That is the refinement worth testing next, and I did not silently add it to this grid to avoid inflating the trial count after the fact.
- 2026-09-19: THE OWNER WAS RIGHT AND I WAS REPORTING WRONGLY. They said repeatedly that it was not possible for every strategy to lose and that I was making mistakes in the backtests. I audited instead of defending, and found three things. (1) MY BIGGEST MISTAKE WAS COMMUNICATION, not code: I kept saying 'fail' and 'nothing passes' when what I meant was 'does not clear the 0.95 deflated Sharpe'. Counted from the registry: of 9,884 holdout-tested candidates, 4,928 (49.9 %) are PROFITABLE on the holdout, 3,497 have profit factor >= 1.2, and 2,617 have PF >= 1.2 with at least 30 holdout trades. The best are XAUUSD:1d PF 3.530 +64.75 % over 32 trades with 9.9 % drawdown, XAUUSD:1d PF 3.363 +100.22 % over 30 trades, XAUUSD:4h PF 3.353 +44.36 % over 34 trades with 5.9 % drawdown. Half of everything tested makes money and I described the whole exercise as failure. (2) REAL BUG, now fixed: BACKTEST_COSTS contradicted its own note. The note said 'about 0.30 spread on a 4,400 price' (0.0068 %) while the constant 0.0004 is applied as a FRACTION and charges 0.04 %, which is $1.76 on that price. Measured MT5's per-bar spread over 132,000 bars (added spread to /api/data/bars, which had been dropping it): XAUUSD median 0.16 = 0.0062-0.0069 %, p90 0.20, p99 0.23; BTCUSD median ~16 = 0.0295-0.0394 %, p90 ~36. So the model was charging 3-6x the real cost, about 0.09 R per trade, which is larger than most expectancies I reported today. Corrected to the p90 spread DOUBLED as a slippage allowance: XAUUSD 0.009 %, BTCUSD 0.09 %. Conservative, measured, and still well under the old numbers. Confirmed against the owner's own live terminal: BTCUSD bid 81683.64 / ask 81700.60 = 16.96. (3) A third bias exists but is SMALL: simulate_orders checks the stop before the target inside a bar, so any bar touching both is booked as a loss - measured at 1 trade in 157 (0.6 %) on gold 4H, so real but not the explanation. ALSO: a plain Donchian-20 breakout on gold 4H holdout returns PF 1.593 and +41.9 % over 157 trades even under the OLD harsh costs, which by itself disproves 'the engine makes everything lose'. LESSON, and it is the important one: never say a strategy 'failed' when it made money and missed a statistical gate. Say what it earned, then say which gate it missed and by how much. The owner had to tell me four times before I checked.
- 2026-09-19: THE OWNER'S RULE WORKS, and both of my errors were mine. They corrected my reading - not 'sweep and close back inside, fade it' but 'if the close is above the previous high buy, if below sell', a CONTINUATION, which my first version explicitly threw away (a test asserted that case was not a signal). Re-tested with both readings in the grid and under the corrected cost model. The fade reading loses on all 18 variants on both markets and its inverse loses too, so it carries no directional information. The continuation reading is profitable on every one of 18 gold variants (PF 1.10-1.47) and beats its inverse every time. But raw it is a regime artefact: search -50.0 % over 310 trades, validation +0.5 %, holdout +40.4 % - fifteen ranging years of losses then gold's 2024-2026 run. ADDING A TREND FILTER, which is mechanically obvious for a breakout, fixes it: lookback 40, 2 R target, EMA400 gives +5.70 % / +11.50 % / +26.69 % - POSITIVE IN ALL THREE WINDOWS over nineteen years and 472 trades, holdout PF 1.378, win 43.2 %, max drawdown 6.76 %, and it beats its own inverse by 62.3 points (inverse -35.60 %, PF 0.628). First candidate of the session positive in every window on full history. What it does not do: deflated Sharpe 0.181 against the 0.95 bar (per-trade Sharpe 0.1343 against 0.2166 needed at 32 trials), and it did not beat holding gold over the holdout, +26.69 % against +73.99 %, though with a 6.76 % drawdown that buy-and-hold did not have. NOTE: this is the first BASELINE row computed under the corrected cost model; every earlier row used costs 3-6x too high. CONNECTION WORTH KEEPING: this rule is essentially a Donchian breakout with a trend filter, and the registry already holds 'close beyond 40-bar high/low, EMA200 trend filter' on XAUUSD:4h at deflated Sharpe 0.895 - so the owner independently described one of the best things the lab had already found.
- 2026-09-19 (afternoon): Spent the session on the owner's demand - 'The need really improvement and correction the backtest mechanisms, the calculations, you need really to work for the strategy'. THREE REAL DEFECTS FOUND AND FIXED, all in the engine rather than in any strategy. (1) Every R the system reported was GROSS: r_multiple divided the pre-cost return by the risk, and because a cost is a slice of the entry price its size in R depends on stop tightness - one variant showed avg_r +0.02 against a true after-cost expectancy of -0.0284. Added net_r to strategy_lab, paper_trader and edge_research (walkforward already had it) and surfaced expectancy_r. paper_trader mattered most because forward_evidence reads whichever R field a strategy writes, so the confidence ladder was crediting costs never paid. (2) Same-bar stop-vs-target was silent: the engine books the stop, which is the safe choice but is a choice. Now counted per trade (ambiguous_exit, target_r) and per split (ambiguous_exits, expectancy_r_bound), with outcome_is_resolved False when the measured expectancy and its bound straddle zero. Measured at about 2 % of trades at 1R and under 1 % at 2R+, so small in general but decisive for marginal 1R strategies. (3) My own defect: the morning's cost correction disarmed the re-score safety net, because rescore_market compares a cost_model TAG and I changed only the constant - 9,880 of 9,884 stored results stayed priced under the old spread under an identical label. COST_MODEL is now v3 and the tag carries the round trip, so this cannot recur. LESSON I NEARLY REPEATED ANYWAY: my first ranking of the re-scored registry filtered on 'rescored_at' being present and produced a confident table that was entirely under the OLD cost model, including an implausible 'XAUUSD:1h, 236 of 236 profitable' that was the tell. Selecting on the cost tag is the only correct filter, and an implausibly clean number is always worth chasing before it is reported. Also opened a paper forward test for the owner's own manipulation-candle rule (sweep_continue_xau_4h): positive in all three windows, beats its inverse by 62 points, zero ambiguous exits, dSR 0.181 against the unchanged 0.95 bar, places nothing. And corrected a stale claim in both CLAUDE.md files - SmartEntry CRT Forward and SmartEntry Daily Agent no longer run from the fallback folder, verified against Task Scheduler.
- 2026-09-19 (evening): Re-scored ALL 9,880 stored Strategy Lab candidates under the corrected cost model and ranked them by earnings instead of by gate outcome. 5,145 (52.1 %) are profitable after costs on their locked holdouts; 2,821 also clear >=30 trades, PF >=1.2, drawdown <=20 % and have a sign the engine did not assign; still ZERO clear the unchanged 0.95 deflated Sharpe bar. THE IMPORTANT PART IS THE DIRECTIONAL TEST I ALMOST DID NOT RUN: gold's profitability is a long bias. 1,074 of 1,125 profitable XAUUSD:4h candidates are long-only, the registry holds no short-only gold candidates at 4h or 1h, and 0 of 978 gold 4h candidates beat buy-and-hold (+74.9 %) - nor 0 of 335 at 1h, nor 0 of 508 at 1d. Every profitable gold candidate is a worse way to hold gold than holding gold. Bitcoin is the opposite and is where the work belongs: BTCUSD:4h buy-and-hold was -3.2 % over its holdout and 1,639 of 3,375 candidates still made money, 661 clearing the honest bar, which a long bias cannot explain. Also found that 2,821 collapses to 1,579 distinct results (largest duplicate group 19), so the count overstates distinct strategies by about 44 %. LESSONS. (1) 'N % of candidates are profitable' is close to meaningless without the buy-and-hold comparison and the long/short split - I reported the percentage first and only caught the long bias because 99.1 % on XAUUSD:1h was too clean to believe. (2) The re-score had to run ONE MARKET PER PROCESS: this machine has 7.4 GB and sits near 86 % used, the parsed registry alone costs 230 MB, and a single 40-minute all-markets run was killed by the OS. Markets themselves are cheap (4-14 MB each); it is the registry plus serialising 59 MB back out that spikes. save_registry writes to .tmp then os.replace, so the kill could not and did not corrupt anything. (3) That kill exposed a real defect, now fixed: a killed run left 'state: running' with a fresh heartbeat, so the lock blocked the next run for up to HEARTBEAT_STALE_SECONDS even though its process was gone. _run_is_active now also frees a lock whose owning pid is demonstrably dead, and only a definite 'dead' frees it - any doubt keeps the lock, because wrongly freeing one would run two searches at once.
- 2026-09-20: Asked to improve the MT4 CRT_Dashboard_EA without touching it. Compared v1, found the defect, built a separate v2, and tested on three independent windows. THE EXIT IS THE DEFECT AND THE PROOF IS ARITHMETIC, NOT A BACKTEST: v1 trails out most trades at a fraction of R while losers pay a full R, because TrailStartPoints 300 ($3.00) and TrailDistancePoints 180 ($1.80) are 0.15 R and 0.09 R against gold's ~$20.42 risk. On the gold holdout that is 91 trailed exits at +0.158 R each against 25 stops at -1.019 R, needing +0.280 R per winner to break even. No filter or timeframe can fix a ratio set by the inputs, which explains why four previous rounds of CRT research all failed. ALSO FOUND a real second bug: v1 derived R from the CURRENT stop, so R collapsed once break-even moved it and every R-based feature rescaled silently. RESULT: every v2 exit beats v1 on gold 2026, BTC 2026 AND unseen gold 2022-2024, where v1 is negative on all three. LESSONS WORTH KEEPING. (1) Testing on a window that never informed a choice is the only way to tell a fix from a fit - the unseen 2022-2024 export (50,001 bars ending six days before the app's history starts) is now a reusable command, 'python -m src.crt_lab unseen'. (2) WHICH v2 exit is best flipped between windows (break-even only on both 2026 windows, R-trail only on 2022-2024) while the v1-versus-v2 gap held in every one. The robust finding is the direction, not the ranking; I chose the default on consistency across all three rather than on the best single number. (3) I had to correct my own design mid-task: v2 shipped with the R-trail enabled and the measurement said removing the trail beats rescaling it. Shipping a change and then testing it is the wrong order only if you refuse to change it afterwards. (4) Still not profitable by this system's bar - best deflated Sharpe 0.352, edge +0.037 R per trade - and bitcoin has no selectable basis at all. Forward evidence next.
- 2026-09-20 (later): Continued the CRT v2 work by asking whether its edge is steady, using the half-year breakdown that previously exposed the M15 EMA50 candidate as regime luck. TWO FINDINGS, one good and one that corrects me. GOOD: v1 is positive in only 1 of 5 half-years on the unseen window while every v2 variant is positive in 4 of 5, all failing only 2022H2 which v1 lost too - so the v1-to-v2 improvement is consistent across periods as well as across windows, which is what the trail arithmetic predicted. CORRECTING ME: I had written that v2 break-even only was 'profitable in absolute terms' on unseen data. Dropping each variant's best half-year shows that was hollow - its +4.99 R total is +6.57 R from 14 trades in July 2024, leaving -1.58 R over the other 121. v1 is the same illusion in reverse (-7.42 R without its one good half). The truthful summary is that v1 destroys value and v2 is roughly break-even: the fix stops the bleeding rather than creating an edge. LESSONS. (1) Always report what survives without the best stretch - a total over a two-year window can be one month wearing a disguise, and net_r_without_best_half is now a permanent field so this cannot recur silently. (2) I did NOT move the default again even though V2_r_trail_only wins this slice (+2.21 R without its best half): choosing the winner of the newest slice each time is precisely the fitting the check exists to catch, and that variant is negative on bitcoin anyway. Consistency across all windows decides, not the latest number. (3) Three separate escaping mistakes in one session writing Python patches through a bash heredoc - backslash-n inside a pattern became a real newline twice and silently corrupted src/crt_lab.py. Use raw strings for any pattern containing an escape, or write the patch to a file first.
- 2026-09-20 (evening): The owner said 'you have plenty of data from mt4,5, you can download any time, that is excuse now for what you saying' - and was right. I had claimed the next evidence had to be forward, having never used the depth already available. Checked it properly: the app serves gold 1h back to 2018 (8.5 years, 50,000 bars) and 4h back to 2007 (19.2 years), while every CRT conclusion so far rested on 2.1 years of M15. M15 itself really is capped - the MT5 broker returns the same single 2022-06-28 bar however early I ask, and the MT4 terminals' own XAUUSD15.hst files hold FEWER bars than I already have - but 1h and 4h depth was there and unused. So I re-ran all 48 higher-timeframe CRT variants under the corrected cost model, since the previous run had been priced 4.4x too harshly. RESULT: the best candidate this system has ever produced, and it still fails. Gold 1h ea_scaled|rr2|none now scores holdout PF 2.02, +27.1 % over 56 trades, deflated Sharpe 0.879 against the 0.95 bar - closer than anything in 21,000 trials - but it LOSES 14.6 % over its five-year search window and is break-even in validation, so it is not positive on all three splits and was rightly rejected before its holdout was read. Its holdout sits entirely in the 2025-2026 gold run. Across all 48 variants and six market/timeframe pairs, nothing passed and nothing was positive on all three splits. THE LESSON I OWE THE OWNER: do not offer a data limitation as a reason until the available depth has actually been exhausted. Here the deeper data did not rescue anything - given 19 years instead of 2 the long histories lose - which is a stronger verdict against these rules than the short sample was, and it is the honest answer to 'nothing survives'. They do not fail for want of data.

- 2026-09-21 ~21:40: "check everything properly" sweep. FOUR bugs fixed (dca27fe, 2cbcd1f).
  (1) THE BIG ONE: automated execution was wired to a confidence gate that was impossible by
  construction. The autonomy gate asks 0.72; confidence was model_accuracy*0.55 + lstm_accuracy*0.35
  + probability*0.10, so with accuracies 0.515/0.564 the BEST POSSIBLE reading over every signal that
  could ever exist was 0.573 (XAUUSD) and 0.603 (BTCUSD), and the 0.55 floor in the same expression
  pinned nearly everything to exactly 0.55. MT4 auto-trading could never have fired on any bar.
  Confidence is now max(p, 1-p). Verified live: BTCUSD 56 / XAUUSD 51, and BTCUSD's alignment gate
  now passes. (2) SmartEntry Daily Agent had been failing every run (exit 1): a numpy bool reached
  json.dumps in ea_monitor._write_json_atomic, which lacked default=str, so the agent aborted and
  wrote nothing. (3) The test suite could not run at all: conftest copies data/ and raced the
  Strategy Lab writing registry.tmp - and since the hourly search now uses its full 32-min slot it
  would have raced most of every hour. *.tmp skipped, copy retries once. (4) A tab in a placeholder
  I had just written, caught reviewing my own diff.
  Also: /auto-trader gained a Broker Accounts panel (MT4 + MT5 selection, live match/MISMATCH
  readout, no password field and there must never be one); and src/demo_sweep_trader.py - LIVE since
  this morning on magic 440805 with a scheduled task - was untracked and is now committed with its
  11 tests.
  STATE: doctor 17 OK / 0 fail; 19 scheduled tasks all result 0; MT4 connected 32768 account
  verified; MT5 pinned to 11581419 and matching; autonomy scanning every 180 s and logging its
  reason.
  OPEN 1 - TEST ISOLATION: tests/test_walkforward_live_engine.py::
  test_every_trade_comes_from_predict_signal_in_live_mode fails ONLY in a batch (asserts 690 == 689)
  and PASSES alone (1 passed, 2:47). Reproduced standalone with the test's own fixture: 689 calls,
  689 distinct times, 689 test_bars, 0 duplicates - an exact match. The engine is sound (each bar is
  predicted from features.iloc[:i+1], folds are an exact array_split partition, training purged by
  LABEL_HORIZON_BARS, and generate_synthetic_data is seeded 42). Some other test in the same process
  adds one predict_signal call. NOT fixed by relaxing the assertion - it is a signal-parity guard.
  Find the polluting module before touching the test.
  OPEN 2 - BTCUSD champion trained on yahoo while the feed is app:mt5:BTCUSD, and BTCUSD is the
  symbol currently passing the alignment gate. src/daily_learning.py already prefers broker bars and
  only fell back because MT5 was unreadable; MT5 is connected and pinned now, so the 05:30 retrain
  should correct it. VERIFY the champion's trained_on after that run rather than assuming.
- 2026-09-22 (TradingView worker): The owner asked twice for "the tradingview live chart" and I twice delivered somewhere else - the system's own /tradingview card, then a Pine script they had to copy by hand. Their words: "you need reall worker ,agent ,bot for the tradingview live chaart extension tv". Built src/tradingview_chart_worker.py (task "SmartEntry TV Chart Worker", hourly :35): it saves the current indicator, board and all, over one named script on TradingView. Four things were wrong on the way and each was caught by looking rather than reasoning. (1) It targeted "SmartEntry Daily Plan"; the indicator actually on their chart is "SmartEntry Market Map", so every cycle would have refused for ever behind a tidy reason. (2) smartentry_market_map.pine had one non-ASCII byte that TradingView renders as "Mixed A. RSI 48.5" - live on their chart since 18 Sep, now ASCII. (3) It ran a separate Chromium: "using wrong broweser". Measured - Edge running with ~30 processes, Chrome not running at all - so it now runs Edge. Attaching to their everyday Edge is impossible: Chromium 136+ ignores --remote-debugging-port on the default profile (restarted Edge with the flag, nothing listened on 9222, no DevToolsActivePort file). Copying their profile would carry the session but also their auth cookies, so a test now fails the build if copytree/Cookies/Local State/User Data appear in that module. (4) The signed-in check looked for a user-menu button and matched ZERO elements on their own signed-in chart - it would have said "not signed in" for ever. Replaced with the redirect, measured both ways: signed in, /chart/ redirects to /chart/<layout id>/; anonymous, it stays on /chart/. 16 tests. STILL UNPROVEN: the worker has never completed a successful cycle - it needs the owner to sign in once in its Edge profile.
- 2026-09-22 (scheduled tasks): Ten SmartEntry tasks failed together at 18:28:55 local with 0x80070520 ERROR_NO_SUCH_LOGON_SESSION, and the demo strategies had not cycled since 15:05 UTC - a ~2.9 h blind spot, including the 13:00 UTC 4H candle. Not a configuration fault: the failing and succeeding tasks have identical principals (Interactive, th_em, Limited), and they all fired at one instant, which is Windows running a batch of MISSED tasks after the machine was asleep or locked. Forcing one task afterwards returned result 0 and cycled normally, so the scheduler is healthy. I ran the four missed demo cycles by hand to catch up: nothing was lost, the 13:00 candle had no breakout, no pullback window and no sweep. The System Doctor DID flag it ("no cycle in the last 2.5 h"), so detection works and no new alerting is needed.
- 2026-09-23 (autostart): The owner said "system auto start is not working" and asked for MT4/MT5 to start with it. Root cause: the machine rebooted at 23:33 on 22 Sep and NOTHING brought the app back - the Startup shortcut is correct and its target exists, but it only fires on an interactive logon. For seven hours all four demo strategies failed hourly with "connection refused" because they ask the running app to do the work. Verified no trade was lost rather than assuming: across every 4H candle that closed while it was down, gold reached 4,358 against a 4,410 breakout trigger and bitcoin 87,160 against 87,831. FIXES, all measured: (1) the System Doctor already ran every 30 min, already checked port 5000 and already reported "nothing listens" - it now restarts the app, which covers a reboot, a crash, the relauncher dying and an OOM kill alike. Proved by killing the app AND its loop, then running the doctor as the scheduler does. Nothing listening is repaired; too many listening never is. (2) scripts\start_everything.ps1 + a "SmartEntry Autostart" logon task start the three required terminals then the app, idempotently, logging to data\system_health\autostart.log - nothing recorded what started at boot, which is why the outage went unnoticed. (3) The doctor now checks terminals BY PATH, not process name: five run here and two are both terminal64.exe. (4) start_trading.bat refuses to become a second server now three things can launch it. All five terminals together hold 689 MB on this 7.5 GB machine - they were suspected of the memory pressure that killed a deep test run and were NOT the cause; Edge held twice that.
- 2026-09-23 (the outage poisoned the models): The learner reads broker candles THROUGH the running app, so with the app down the 05:30 retrain silently fell back to Yahoo, tagged itself "yahoo_fallback" - and PROMOTED a new BTCUSD champion on it. An outage replaced the live model with one fitted to prices the account cannot trade and nothing objected, because the source rule only faced one way: it promoted a broker challenger over a non-broker champion but never refused a non-broker challenger. Added the missing half. Training still happens and is recorded - only the live swap is refused, and the message says so, or it reads as learning having stopped. Repaired with the system's own override: BTCUSD went back to broker on the source-match rule, XAUUSD promoted on a genuine gain (+4.621% vs -1.296% after costs on 568 unseen bars, drawdown 2.1% vs 3.1%). That repair then exposed a second gap - the documented off-schedule override tripped the model-integrity check ("something other than Daily Learning wrote models/"), a true alarm about an authorised act, which is the kind that teaches people to ignore alarms. Such runs now log what they wrote by content hash. MY OWN ERROR TO NOT REPEAT: I twice read a status payload for a key it does not have ("positions" on the VTB endpoint, which uses open_trade/open_positions) and both times nearly reported a broker-versus-system discrepancy that did not exist. VTB is tracking its open leg correctly: realised 60.66 + 15.28 banked on the closed leg + 9.10 floating = 69.76.
- 2026-09-24 (INCIDENT, self-inflicted): My own diagnostics took the live system down in two ways and I only found it because the owner asked me to check MT4. (1) The 08:52 probe into the flat probability output called `train_model(feats, sym)` to compare raw vs calibrated output - and train_model PERSISTS to models/. It overwrote the live XAUUSD and BTCUSD champions with models fitted to 70 % of H4 bars inside a test. Caught by the doctor's model-integrity check (the guard earned its keep), restored from the 04:30 archives written by the scheduled learning run, and logged in data/model_restores.json with the reason. RULE: never call train_model, LSTMTrader.train or anything under src.train in a diagnostic - they write to models/. Build the estimator directly (RandomForestClassifier(...).fit) when measuring. (2) Repeatedly calling mt5.initialize()/mt5.shutdown() from throwaway scripts against the SAME terminal the app uses disrupted the app's connection: gold_session_pullback and volatility_trend_breakout both halted at 10:01 and 10:03 with "MT5 error: account details unreadable" and stopped trading for about two hours. Resume needs the control secret (POST /api/demo-trading/resume and /api/demo-breakout/resume with X-Control-Secret). RULE: MT5 diagnostics compete with the live app for the terminal - expect halts, check for them afterwards, and prefer reading through the app's own endpoints where one exists.
- 2026-09-24 (MT4 bridge, second outage in two days): The bridge can be BOUND BUT SILENT - PID 26160 (ICMarketsSC-Demo01, 12755139) held 32778/32779/32780 while never answering a HEARTBEAT, even at a 9 s timeout. The doctor reports this correctly but confusingly: "Broker connections: MT4 bridge disconnected" sits beside "MetaTrader terminals: All 3 required terminals are running", both true, because the terminal check tests the PROCESS and the broker check tests the BRIDGE. Re-attaching the EA on the chart is what fixed it yesterday; a running terminal proves nothing about whether its bridge answers.
- 2026-09-24: The doctor could report "MT4 bridge disconnected" and "All 3 required terminals are running" in the same report, because check_brokers tests the BRIDGE and check_terminals tests the PROCESS. Both lines were true and together they read as a contradiction. run_doctor now keeps the broker result and passes it to check_terminals, which downgrades its OK to a WARN naming the exact state - process up, bridge silent - and the only repair that works (remove the DWX EA from its chart and attach it again inside MT4). tests/test_doctor_bridge_heartbeat.py pins it, including that a genuinely missing process still FAILs with its own remedy and that a failed broker check cannot create a false alarm.
- 2026-09-24: Added random_entry_benchmark() to src/walkforward_backtest.py (off by default, random_draws=0) to answer a question the system could not previously ask: is a backtest's return prediction, or drift? The daily learning gate promotes on after-cost return, and gold rose 242 % over the 8-year H4 window, so a mostly-long model banks a large positive return with zero forecasting ability. The control is a PERMUTATION, not a coin flip: each draw keeps the model's exact signal count and long/short mix and only shuffles WHICH BARS they land on, holding stop/target distances, the one-position rule, the hold limit, costs and bars identical. A coin-flip control would randomise the direction mix too, so a permanently-long model in a rising market would beat it every time and the test would report drift as skill. Validated in both directions in tests/test_random_entry_benchmark.py: a deliberately clairvoyant entry IS detected as skill (p<=0.05), and an always-long strategy in a rising market makes money and is correctly reported as NO skill. Bars come from the app's /api/data/bars (its own MT5 connection) rather than a second mt5.initialize().
- 2026-09-24: Ran the permutation benchmark on the self-learning models (results in BASELINE.md). Gold is WORSE THAN CHANCE at the 0.6th percentile on expectancy per trade over 254 trades - 99.4 % of random arrangements of its own signals beat the one it picked. Bitcoin shows nothing either way on 50 trades. IMPORTANT METHOD NOTE for any future use: decide on expectancy per trade, not total return. The draws do not open equal numbers of trades, because the model's signals arrive in clusters that the one-position-at-a-time rule absorbs while shuffling spreads them out (gold: 254 model trades vs 435 random average). In a market that rose 242 % the extra exposure alone shifts total return, so total return would have overstated the gap. Here both measures agreed, which is what makes the gold result safe to state.
